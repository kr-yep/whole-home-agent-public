# Whole Home Agent — Public Repository State

**Checkpoint:** `PUBLIC-B1-M20-001`

**As of:** `2026-09-01 Asia/Taipei`

**Governance:** `PROPOSED — NOT ADOPTED`

**Runtime operation:** `OPERATE DISABLED`

This file is the live checkpoint for the clean public repository. It records current scope and evidence; it grants no household, data, device, account, or operational authority.

## Current direction

- Publish and collaborate on the smallest viable offline B0 semantic replay baseline plus one bounded B1 prerecorded replay.
- Keep B0/B1 inputs limited to synthetic or separately licensed, lawfully reusable public fixtures.
- Keep the product slice limited to the fixed project-generated clip; use the local-only, hash-pinned VISOR sparse-frame set strictly as a non-commercial detector screen behind a separate adapter.
- Use the local-only, hash-pinned VOST consecutive-frame subset strictly as a non-commercial scheduler/cost screen; it is not a product claim source.
- Reuse the implemented B0 `ClaimCandidate` → commit → projection → query path as the only semantic state path for B1.
- Exclude live/private sensing and all physical or external action from the current scope.
- Do not assume a graph, Memory Core, multi-agent runtime, or durable database is necessary.

## Authority and roles

| Role | Assignment | Current authority |
|---|---|---|
| Repository owner | GitHub account `kr-yep` controls repository settings | Repository administration only; not household or policy authority |
| Project Owner | `UNASSIGNED` | None until explicitly assigned |
| Policy Owner | `UNASSIGNED` | None until explicitly assigned |
| Engineering Lead | `UNASSIGNED` | None until explicitly assigned |
| Data Steward | `UNASSIGNED` | None until explicitly assigned |
| Household roles / affected persons | `UNDEFINED` | No consent or operational authority exists |
| Runtime identity | `NOT CREATED` | No capability or credential exists |

## Document status

| Artifact | Status | Meaning |
|---|---|---|
| `AGENTS.md` | `PROPOSED` | Conservative interim repository instructions |
| `ACTION_POLICY.md` | `PROPOSED — NOT ADOPTED` | All sensing, private-data, egress, device, and physical operation disabled |
| ADR 0001–0005 | `PROPOSED` | Design candidates, not automatically adopted requirements |
| B0 implementation and fixtures | `IMPLEMENTED / VERIFIED IN DECLARED TEST ENVELOPE` | A bounded semantic replay slice exists; its frozen golden semantic hash is unchanged |
| B1 candidate-source seam and run receipt | `IMPLEMENTED / VERIFIED WITH SYNTHETIC CONTRACT TESTS` | Generic finite source contract, provenance types, fail-closed run outcome, and B0 compatibility remain the sole semantic admission path used by the later prerecorded adapter |
| B1 generated-video manifest, PTS decoder, and scheduler | `IMPLEMENTED / VERIFIED ON ONE SYNTHETIC REPLAY` | Hash-pinned allowlisted H.264 source revision 2, exact PTS/time-base decode, and motion-plus-periodic frame selection feed the later bounded perception source; revision 1 remains in Git history |
| B1 perception/tracking/evaluation baseline | `IMPLEMENTED / VERIFIED ON SYNTHETIC REPLAY PLUS BOUNDED PUBLIC SCREEN` | Canonical detections, test-only annotation ceiling, RGB smoke detector, clip-local tracker, fixed quality/cost evaluator, RF-DETR translation seam, and paired torchvision public-data baselines exist; indoor evidence is limited to the separately described VISOR screen |
| VISOR screen v1 and paired public baselines | `IMPLEMENTED / VERIFIED IN A BOUNDED LOCAL ENVELOPE` | Three source-video-split VISOR sequences, strict license/hash manifests, mask-to-box adapter, source-index timing, SSDLite320 and RetinaNet-FPN adapters, one frozen-test lock, and paired quality/cost receipts exist; source data and weights remain ignored and are not distributed |
| Sliced SSDLite validation gate | `IMPLEMENTED / REJECTED BY FROZEN DEVELOPMENT/VALIDATION EVIDENCE` | Four bounded overlapping tiles, original-coordinate translation, wrapper provenance and label-aware NMS were tested without a test-split path; validation quality fell and p95 exceeded the gate, so this candidate is stopped |
| VOST motion screen v1 | `IMPLEMENTED / VERIFIED IN A BOUNDED LOCAL ENVELOPE` | Two upstream-split VOST sequences, range-only acquisition, upstream license/split evidence, source-offset/5 fps replay semantics, development-only scheduler selection, and paired full-frame/scheduled FPN receipts exist; the validation gate passed, while target detection and fixed-camera transfer remain unproven |
| VOST target-tracking screen v1 | `IMPLEMENTED / VALID DEVELOPMENT RUN / REJECTED ON DEVELOPMENT` | A separate bottle-labeled train/validation substrate, mask-to-box target contract, target-event detection coverage, and clip-local tracker gate exist. The development source failed the predeclared quality/tracking gates, so the reserved validation source was not run and movement-candidate work is stopped for this observation path |
| VOST M11 failure localization v1 | `IMPLEMENTED / VALID DEVELOPMENT-ONLY DIAGNOSTIC` | One clean instrumented development pass separated absent/void, missing proposal, confidence-filtered, localization, and matched frames while keeping low-score proposals outside canonical detections. A separate mask-box oracle exposed an independent greedy-IoU association failure; reserved validation remained unloaded |
| VOST M12 RF-DETR Small screen v1 | `IMPLEMENTED / VALID DEVELOPMENT RUN / REJECTED ON DEVELOPMENT` | One immutable RF-DETR Small 1.9.4 adapter path fixed sparse COCO mapping, normalized the product threshold, and executed under a one-attempt offline guard. It improved the finite-sequence match count but missed the unchanged recall gate; the two cost gates passed and reserved validation remained unread |
| M13 D-FINE Small synthetic qualification v1 | `IMPLEMENTED / ENGINEERING COMPATIBLE / NO DEVELOPMENT SCREEN` | One immutable community-converted Safetensors path fixed dense COCO mapping and a closed offline loader, then passed the generated-input determinism and cost checks in one valid real-load attempt. Converted-weight parity and target-source accuracy remain unverified, so VOST was not read and the candidate is not scientifically selected |
| M14 detector scientific-priority reality gate | `IMPLEMENTED / STOP MODEL SWAPPING` | Official primary evidence for exactly D-FINE Medium and RT-DETRv2 Small was compared without model or media loading. Neither supplied a material same-protocol prior over the failed target-screened path; a config coverage omission was recorded fail-closed without changing thresholds |
| M15 target-domain substrate reality gate | `IMPLEMENTED / ZERO ELIGIBLE / PIVOT` | Official primary evidence for exactly HOMAGE, CAD-120, and Watch-n-Patch was checked without media acquisition. None passed every localization, terms, acquisition, split, and bounded-cost requirement, so no dataset was selected and no acquisition or training was authorized |
| M16 target-label-oracle feasibility gate | `IMPLEMENTED / BOUNDED PASS` | A no-media D1 scope layer now validates complete scored/unknown frame accounting, visibility, persistent identity, reference transitions, and protected source groups before reusing the existing quality evaluator. All exact synthetic metric/rejection cases passed; this establishes no media/model/transfer quality |
| M17 generation-strategy reality gate | `IMPLEMENTED / ONE BOUNDED SELECTION` | Exactly the existing vector renderer, Blender headless 3D, and Kubric licensed-asset routes were compared without installation, download, or generation. Only the existing project-owned route passed every frozen feasibility gate; selection authorizes one tiny M18 D1 slice, not training or a transfer claim |
| M18 tiny vector D1 slice | `IMPLEMENTED / BOUNDED PASS` | Three project-owned protected groups now contain 18 PNG/annotation pairs plus one exact M16 fixture. Hash manifests, two-run byte identity, six reference transitions, 0.1–1% targets, and unchanged old golden hashes passed; this is substrate-mechanics evidence, not detector-transfer evidence |
| M19 real transfer-oracle reality gate | `IMPLEMENTED / YCB-VIDEO BOP'19 SELECTED` | The frozen no-download AND matrix leaves GMU ineligible because its official dataset route is unavailable. HB and YCB-V pass through BOP; the final cost tie-break selects only the 660 MB YCB-V BOP'19 route for one acquisition/D1 translation slice |
| M20 YCB-V BOP'19 acquisition/translation | `IMPLEMENTED / NORMAL PRE-EXTRACTION STOP` | Both immutable archives matched frozen sizes and hashes. Materialization stopped before extraction because base is rooted at `ycbv/` but test at `test/`, contradicting the frozen single-root rule. No real annotation or image was read |
| B1 binding/relation/query slice | `IMPLEMENTED / VERIFIED ON ONE SYNTHETIC REPLAY` | One-instance binding, conservative containment/zone assertion and retraction rules, typed abstentions, estimated candidates through the unchanged committer, relation evaluation, and evidence-traceable `key → bag → sofa` query exist; no real indoor transfer evidence exists |
| B1 CLI and Streamlit presentation | `IMPLEMENTED / VERIFIED ON ONE SYNTHETIC REPLAY` | A closed composition/presentation boundary exposes the fixed clip, scoped answer, evidence, abstentions, metrics, diagnostics, and receipt; it accepts no upload, camera, arbitrary path, free-form query, credential, or action handle |
| Python package distribution | `IMPLEMENTED / VERIFIED BY LOCAL CLEAN INSTALL` | The wheel contains only the fixed D0 replay, its configs, and generator provenance; a fresh Python 3.12 environment ran the CLI from outside the checkout |
| `docs/b0-b1-architecture-plan.md` | `PROPOSED — NOT ADOPTED` | Smallest B0 → B1 boundary, quality scenarios, gates, and deferred complexity |
| ADR 0005 and `docs/evaluation/visor-screen-v1.md` | `PROPOSED DESIGN / VERIFIED BOUNDED EVIDENCE` | Public sparse-frame evaluation design and the exact Reality Gate findings; neither grants sensing, product, or policy authority |
| B0 → B1 Archify diagrams | `VERIFIED RENDERING OF THE IMPLEMENTED BOUNDED SLICE` | 9/9 showcase and visual delivery passed; rendering evidence does not adopt governance or expand the implementation envelope |
| MIT `LICENSE` | `SELECTED FOR PUBLIC RELEASE` | Applies to original repository code and documentation |

## Evidence envelope

- The public export contains the B0 package, synthetic semantic fixtures, one project-generated CC0 prerecorded clip with manifests/annotations, B1 adapters, public asset/hash configs, tests, presentation code, architecture/evaluation documents, and proposed governance. It does not contain VISOR/VOST source bytes or model weights.
- It intentionally excludes prior private Git history, coursework/competition experiments, third-party datasets, household/private media, model weights, run outputs, local databases, environment files, and credentials.
- Existing tests exercise semantic replay, provenance, idempotency, conflict/unknown handling, malformed input, boundary restrictions, media allowlisting and timing, detector/tracker/evaluator contracts, conservative relation inference, partial-source failure without a queryable session, and the fixed CLI/Streamlit presentation.
- Local M8 verification used Python 3.12.13 with bytecode writes disabled: `80/80` unit, contract, integration, audit, CLI, Streamlit, VISOR/hash/source-index, torchvision-translation, and sliced-wrapper tests passed. The frozen B0 semantic hash remains `226d30a5b826720d607d0b9a29bf3dfb9f5429eeedbbd70ffd1ff23c21233c8f`.
- Local M9 verification used the locked project Python 3.12.13 environment with bytecode writes disabled: `84/84` unit, contract, integration, audit, CLI, Streamlit, VISOR, VOST manifest/hash/split/source-index, motion-coverage, torchvision-translation, and sliced-wrapper tests passed. The staged public-release audit scanned 109 files / 218 index-and-worktree snapshots with zero violations and `operate_enabled: false`.
- Local M10 verification used Python 3.12.13, torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, and the RTX 4070 Laptop GPU. `85/85` tests passed before the clean development run. The staged implementation audit scanned 112 files / 224 index-and-worktree snapshots with zero violations and `operate_enabled: false`.
- Local M11 implementation verification used Python 3.12.13. `90/90` unit, contract, integration, audit, CLI, Streamlit, VOST, diagnostic-seam, mutually-exclusive-classification, and detector-priority tests passed before and after the clean diagnostic run. The final staged public audit scanned 119 files / 238 index-and-worktree snapshots with zero violations and `operate_enabled: false`; the offline source/wheel build also passed. The diagnostic used torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, and the RTX 4070 Laptop GPU.
- Local M12 verification used Python 3.12.13. `96/96` unit, contract, integration, audit, CLI, Streamlit, VOST, sparse-COCO, strict-gate, offline-guard, and one-attempt tests passed before the implementation commit and again after the result documentation. The final staged public audit scanned 124 files / 248 index-and-worktree snapshots with zero violations and `operate_enabled: false`. The candidate used torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, RF-DETR `1.9.4`, CUDA `12.8`, cuDNN `91900`, driver `610.74`, and the RTX 4070 Laptop GPU; the current `uv.lock` does not reproduce that CUDA package set.
- Local M13 pre-attempt verification used Python 3.12.13 and passed `110/110` unit, contract, integration, audit, CLI, Streamlit, VOST, D-FINE fake-output, dense-COCO, import-isolation, generated-fixture, offline-guard, p95, and one-attempt tests; the post-result suite passed `111/111`. The final staged public audit scanned 131 files / 262 index-and-worktree snapshots with zero violations and `operate_enabled: false`. The sole valid real-load attempt used torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, Transformers `5.16.1`, Safetensors `0.8.0`, CUDA `12.8`, cuDNN `91900`, and the RTX 4070 Laptop GPU; the current `uv.lock` is explicitly recorded as not reproducing that GPU package set.
- Local M14 verification used the locked Python 3.12.13 environment and passed `117/117` unit, contract, integration, audit, CLI, Streamlit, VOST, detector-adapter, no-media priority, scope-gap, and boundary tests. The final staged public audit scanned 137 files / 274 index-and-worktree snapshots with zero violations and `operate_enabled: false`. M14 performed no model or media load and added no runtime dependency or adapter.
- M15 used only official dataset pages, papers, terms, author pages, and endpoint metadata. HOMAGE failed the per-frame localization requirement because its scene graph protocol samples 3 or 5 frames per atomic interval and covers interacted objects; CAD-120's accessible official use terms and current acquisition path were not established; Watch-n-Patch did not establish per-frame reference object boxes and likewise lacked verified current official terms/acquisition. `UNKNOWN` remained distinct from factual `FAIL`, but both were selection-ineligible under the frozen AND gate. The locked Python 3.12.13 environment passed `126/126` tests, and the staged public audit scanned 143 files / 286 index-and-worktree snapshots with zero violations and `operate_enabled: false`. The bounded result is zero eligible and a pivot to a no-media D1 target-label-oracle feasibility gate; no media/archive/model load, training, source read, movement candidate, claim, or operational capability was added.
- M16 introduced one pure D1 target-oracle boundary and one hash-pinned `D0_SYNTHETIC` semantic fixture while reusing the existing canonical box/detection/quality calculator. Perfect, empty, duplicate, wrong-class, bad-localization, explicit-negative-frame, ordering, unknown, identity-conflict, and every protected split-group case produced the frozen exact result or failure code. On clean implementation revision `7c2f50425590de39cdb2d62ebb37d0ed9c529b30`, Python 3.12.13 passed `138/138` tests and the public audit scanned 147 files / 294 snapshots with zero violations. The final staged result suite passed `140/140`, and the audit scanned 151 files / 302 snapshots with zero violations and `operate_enabled: false`. No image/video was generated or downloaded; no model, tracker, training, private/reserved source, movement candidate, semantic claim, or operation was introduced.
- M17 compared the three frozen generation strategies using repository evidence and official tool/asset capability, license, and distribution sources only. The existing 2D renderer was uniquely eligible: it already owns its artwork, represents stable key/bag/sofa identities and two transitions, renders the 28×12 key at about 0.1458% of a 640×360 frame, and records reproducibility hashes. Blender and Kubric document richer capabilities but retained selection-ineligible integration/delivery unknowns; Kubric's canonical network/container/cloud-asset route also conflicts with the frozen no-cloud gate. On clean result revision `1e3e4a1ae12e9f8f2156148620d6b889770a2842`, Python 3.12.13 passed `149/149` tests and the public audit scanned 157 files / 314 snapshots with zero violations and `operate_enabled: false`. No dependency/tool was installed, no asset/media was downloaded or generated, no model was loaded, and no training or operational capability was introduced.
- M18 generated exactly three project-owned protected source groups with 18 640×360 PNG/annotation pairs and one exact M16 fixture. Each split contains visible 0.1458%-area keys, truncated/occluded/absent/unknown states, an explicit scored negative, and containment/location reference transitions. Two clean Windows generations matched every committed output and manifest byte; Linux CI later showed that PNG bytes differ across platforms while two Linux generations remain internally identical. Cross-platform PNG byte identity is therefore not established. Total non-manifest output was 103,957 bytes and the old B1 generator/media/annotations/manifest hashes remained unchanged. On clean implementation revision `a7ea0b8582558c293307e727c703bcb059c6613a`, Python 3.12.13 passed `161/161` tests and the public audit scanned 198 files / 396 snapshots with zero violations; the pre-CI result suite passed `164/164` and audited 201 files / 402 snapshots with zero violations and `operate_enabled: false`. No external asset, new dependency, model, training, private/reserved source, claim, or operational capability was used.
- M19 checked exactly GMU Kitchens, HomebrewedDB, and YCB-Video using current official author/institution papers, dataset pages, licenses, archive listings, BOP format/task documentation, and toolkit parameters. GMU's author-cited data URL now redirects to the department home page, so current terms, acquisition, and safe complete scoring are not established. HB and YCB-V both pass the frozen feasibility gates through BOP. The first two tie-breaks remain tied; YCB-V wins the final cost tie-break because its annotated BOP'19 route is displayed as 660 MB versus 2.35 GB for HB's smallest public annotated route. On clean result revision `380ebf18fbb463af28ba975ddcceaff3cbfeccef`, Python 3.12.13 passed `173/173` tests; the final staged public audit scanned 207 files / 414 snapshots with zero violations and `operate_enabled: false`. No archive, media, annotation, model, or reserved local source was read; no training, candidate, claim, or operation occurred. The 0.1–1% target is an executable post-acquisition pixel-count check, not a preverified observation.
- M20 froze its contract at `2a69db3` and its synthetic translator/archive guard at `769c2ae` before downloading any real data. Both selected archives from immutable BOP revision `5c2c4aa229800355648cd268040aa814f8dc94f0` matched the exact frozen byte counts and SHA-256 values. Header preflight then found `ycbv/` in the base archive and `test/` in the test archive, so the frozen single-root requirement stopped the run before extraction. Zero real annotation/image files were read, and no D1 slice, model, prediction, training, candidate, claim, relation, or operation was produced. On clean result revision `ff4368eba2651f1a7aa182039c40197b1bbdf738`, Python 3.12.13 passed `188/188` tests and the public audit scanned 218 files / 436 snapshots with zero violations and `operate_enabled: false`. The local archives remain under the ignored `data/external/` root for one separately frozen infrastructure repair.
- GitHub Actions run [33486414127](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33486414127) passed every job except Python 3.11 because one test fixture used Python 3.12's generalized nested f-string grammar. The fixture now builds its TOML blocks before interpolation; local `ast` parsing under the 3.11 grammar and the focused tests passed. Follow-up run [33486674987](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33486674987) completed successfully across Python 3.11–3.14, the prerecorded-video contract, and the closed public demo.
- An isolated offline M9 package build produced the source archive and universal wheel and included the new Python adapter/evaluator modules without including VOST/VISOR data, model weights, local receipts, or evaluation configs/tools in the installed runtime data bundle.
- Source revision 2 is an 80-frame H.264/yuv420p generated replay with SHA-256 `b9cc79476d77f8d45acd1803c924de73914ffc4790f4da271f77cc8d4742eb43`. It was versioned because revision 1's encoding failed Chromium playback. Two consecutive local generations matched; browser QA then showed duration 8 seconds, ready state 4, scoped revision-2 content, and zero console errors.
- The source-revision-2 RGB smoke baseline measured AP50/key recall `1.0`, mAP50:95 about `0.7293`, zero false positives, zero clip-local ID switches/fragmentations, relation event F1 `1.0`, and the expected two-step answer. These are generated-artwork measurements only.
- `uv build` produced a source archive and universal wheel. A new Python 3.12 environment installed `whole_home_agent-0.1.0-py3-none-any.whl[demo]`, then ran `demo-recorded` outside the checkout with a `COMPLETE` receipt, two accepted estimated claims, and `FOUND sofa` under `source:b1-key-bag-sofa@2`.
- GitHub verified `kr-yep/whole-home-agent-public` as public with `main` as its default branch. Seed commit `21d057569073e7f2e16631780a8ab1150c2920f9` contains the clean release history.
- GitHub Actions run [32803963091](https://github.com/kr-yep/whole-home-agent-public/actions/runs/32803963091) completed successfully for the seed commit across configured Python 3.11–3.14 jobs.
- M2 through the first M6 push had green B0 jobs but failed the Linux video/demo jobs because Windows-generated annotation bytes used CRLF before Git normalized them to LF, invalidating the manifest hash in CI. Run [33447377312](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33447377312) exposed the same failure after the M6 push. The generator now emits canonical LF, and a regression test checks both generated text artifacts.
- GitHub Actions run [33447921546](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33447921546) completed successfully for canonical-LF commit `4117e933b4b6076a5cda9130f61ea09ec998934b`: Python 3.11–3.14 B0 jobs, the prerecorded-video contract job, and the closed-demo job all passed.
- Archify v2.16.0 produced the proposed system and data-flow diagrams. Both passed 9/9 showcase checks with zero composition errors/warnings, desktop containment at 1440×900 through 2048×1320, and human inspection of light/dark captures. Exact source/artifact hashes and evidence limits are recorded in `docs/b0-b1-architecture-plan.md`.
- VISOR screen v1 froze `P01_03` (development, 61 frames), `P03_11` (validation, 29), and `P14_05` (test, 22) before results. Official Bristol archives and annotations passed full size/SHA-256 verification. The adapter preserves source frame indexes, maps only explicit COCO-compatible labels, and reports six boundary-clipped polygon points across five mapped objects under an 8-pixel tolerance without rewriting source data.
- On the RTX 4070 Laptop GPU, RetinaNet ResNet50 FPN v2 improved validation recall@0.5 from SSDLite320's `0.1429` to `0.2500`; validation 0.1–1% area recall moved from `0/3` to `1/3`. Detector p95 moved from about `47.4 ms` to `71.7 ms`, and peak VRAM from about `85.7 MiB` to `393.3 MiB`. The first and now locally locked test run improved overall recall from `0.1429` to `0.3929`, but both missed the test's only 0.1–1% target. VISOR active-object labels are not exhaustive scene labels, so generic-detector false positives and AP are not treated as exhaustive precision evidence.
- The validation-only four-tile SSDLite candidate reduced validation recall from `0.1429` to `0.0357`, left 0.1–1% target recall at `0/3`, and increased detector p95 from about `49.0 ms` to `243.5 ms`; peak VRAM stayed about `85.7 MiB`. It failed both the quality and `200 ms` cost gates and is rejected without touching the frozen test.
- VOST motion screen v1 pins `8253_open_bag` (development, 66 frames) and `5182_open_box` (validation, 136) from the official train/validation partitions. Its downloader verified the public S3 object version/ETag, a `21,042,989`-byte central directory with SHA-256 `7685955fd97adaade763a9a8f667ab8589b8fd63bac596eb207697ac68ed6d34`, and 404 selected JPEG/mask files totaling `52,172,072` bytes with canonical manifest SHA-256 `3a4b440fd2e4d5d45363a9daf2c986799ca14a9bee0578a29f910ae41ec7863f`. It also retains ignored upstream license, README, and split-list evidence; no third-party bytes enter Git.
- Development-only thresholds `0.005`, `0.01`, `0.02`, and `0.03` each selected `33/66` frames with 100% same-or-following mask-change coverage, so the deterministic tie rule froze `0.03`. On validation, scheduled RetinaNet-FPN reduced calls from `136` to `52` (`61.8%`), covered `40/41` mask-change events within one 5 fps frame (`97.6%`), measured `65.15 ms` detector p95, `0.58 ms` scheduler p95, about `352.2 MiB` peak VRAM, and pipeline real-time factor `0.16` versus `0.35` full frame. All declared bounded gates passed.
- The VOST result is scheduler-selection evidence only. Exact-frame validation coverage was `43.9%`; both sources are egocentric; the selected bag/box mask is not a COCO target-detection contract; and no object identity, containment, physical movement truth, fixed-camera transfer, live stream, or household claim is established.
- M10 froze 240 additional local-only VOST JPEG/mask files totaling `39,189,582` bytes with canonical hash `1e9f53eb1c0e302a0eea54cb5d9bff86d5029ee207b6bb7168c6260a199de060`. The official-train development source `3518_unscrew_bottle` contains 51 paired frames; the official-validation source `3510_unscrew_bottle` contains 69 and remained reserved. Fixed sampled offsets visually aligned target mask ID 1 with the visible bottle; this is an agent precheck, not source-authored COCO class truth.
- On clean code revision `581507229d43808fa6e4072b8b82ad98f8946268`, development full-frame recall@0.5 was `0.1961`, AP50 `0.1436`, and mAP50:95 `0.0808`; the tracker recorded five ID switches and four fragmentations. Scheduled inference covered `0.2051` of target mask-change events within one frame versus `0.2821` full-frame, retained `0.7273`, and avoided `0.4902` of calls. Detector p95 was `73.76 ms`, scheduler p95 `0.74 ms`, peak VRAM `412,383,744` bytes, and full/scheduled real-time factors were `0.416/0.246`. The predeclared development gate returned `REJECT_ON_DEVELOPMENT`; validation was not run and is not a zero score.
- On clean code revision `eb5d28a3bbd3bf552fb3e8d0bb00e8bc44c19e81`, the one-pass M11 diagnostic classified all 51 development frames: zero absent/void, zero without a bottle proposal at or above `0.05`, 30 with proposals only below `0.25`, 11 with product-threshold proposals but IoU below `0.50`, and 10 matches. Sixteen low-score frames were IoU-matchable, making the threshold-only matched-frame upper bound `26/51 = 0.5098`, below the unchanged `0.60` gate. The mask-box oracle tracker matched 51/51 observations for evaluation but produced 16 ID switches and zero fragmentations. Reserved validation was not loaded or run.
- On clean code revision `4aa0a2ad87dd100be5b38f0db5a01082ab023eb9`, the only M12 RF-DETR Small development attempt was valid. It matched `25/51 = 0.4902` target boxes versus the M10 full-frame comparator's `10/51 = 0.1961`, measured AP50 `0.2762`, mAP50:95 `0.1792`, `49.7820 ms` detector p95, and `149,987,840` bytes peak allocated VRAM. Recall failed the predeclared `0.60` gate while both strict cost gates passed, so the candidate is stopped. Paired descriptively, it matched 16/30 prior confidence-filtered frames, 0/11 prior localization-miss frames, and 9/10 prior matches. No second attempt, training, test source, reserved image/mask read, validation, movement candidate, claim commit, live/private/cloud/action connection, or `OPERATE` enablement occurred.
- On clean code revision `a01799b52ce4ffeeb239bbe6eb397afa2ff64bcf`, the only M13 real checkpoint-load attempt was valid. A community-converted D-FINE Small Safetensors artifact completed one excluded warm-up and 51 measured calls on one frozen in-memory generated RGB input. Canonical output was constant; p95 was `58.8077 ms`, maximum `62.4707 ms`, and peak allocated VRAM `104,530,432` bytes. Both strict cost gates passed with zero recorded socket attempts and zero public/private media bytes read. This proves only synthetic engineering compatibility: author-checkpoint parity, target-source accuracy, indoor transfer, stable runtime, tracking, movement, and product suitability remain unmeasured. The decision is `STOP_DFINE_SMALL_NO_DEVELOPMENT_SCREEN`; VOST/VISOR bytes remained unread.
- M14 used only official paper, repository, release-log, license, and artifact metadata. D-FINE Medium passed the narrow D-FINE-S same-table subgate (`+3.8` AP75, `+4.1` AP-small), while RT-DETRv2 Small failed it (`-0.5/+1.1`). Neither supplied a material same-protocol comparison against the target-screened RF-DETR Small path, and generic COCO proxies do not establish fixed-camera indoor transfer. The frozen TOML's omission of that Goal-level comparator was recorded as `FAIL_CLOSED_SCOPE_GAP`; no threshold was changed. The bounded result is `STOP_MODEL_SWAPPING`, with zero new model downloads/loads, media reads, screens, training runs, movement candidates, claim commits, or operational capability.
- Passing tests supports only the tested code, fixtures, interpreter, and environment. It does not support CV accuracy, household transfer, real-time performance, privacy compliance, production readiness, or physical truth.

## Recorded directions

| ID | Direction | Status |
|---|---|---|
| `PUB-DIR-001` | Create a separate clean public repository; do not expose or rewrite the private repository's history | Active |
| `PUB-DIR-002` | Keep `OPERATE` disabled until roles, consent, risk classification, safety boundaries, enforcement, and separate activation are complete | Active |
| `PUB-DIR-003` | Start from a minimal B0 baseline; separate data, control, action, authority, and physical outcome | Active |
| `PUB-DIR-004` | Use MIT for original public repository code/docs; review third-party artifacts separately | Active |
| `PUB-DIR-005` | Keep B0 as the only claim-commit/query core; make prerecorded perception a replaceable candidate-producing adapter | Implemented and locally verified on one project-generated synthetic replay; no real indoor transfer claim |
| `PUB-DIR-006` | Deliver a reproducible public demo, installable package, implemented-code diagrams, and release audit without widening sensing or action scope | Implemented, locally verified, and verified by public CI within the recorded envelope |
| `PUB-DIR-007` | Acquire a small lawful public indoor substrate, freeze source-video splits before results, compare paired real detectors, and keep all third-party bytes out of Git | Implemented and locally screened within the VISOR CC BY-NC non-commercial envelope; no product-transfer claim |
| `PUB-DIR-008` | Acquire a lawful consecutive public substrate without the full corpus, select scheduler settings on development only, and measure validation change coverage versus FPN cost | Implemented and locally screened within the VOST CC BY-NC-SA non-commercial envelope; no target-detection, fixed-camera, or product claim |
| `PUB-DIR-009` | Before creating movement candidates, freeze an explicit VOST bottle mask-to-label development/validation gate and reject the observation path if target detection or clip-local tracking is inadequate | Implemented; rejected on development without running validation. This stops only the frozen RetinaNet/IoUTracker/scheduler path on the declared source |
| `PUB-DIR-010` | Localize the rejected M10 development path with one non-product low-score diagnostic view and a separate mask-box tracker oracle before choosing a replacement | Implemented; confidence filtering was the largest detector symptom, threshold-only inclusion cannot meet the existing gate on retained outputs, and the oracle exposed an independent tracker blocker |
| `PUB-DIR-011` | Freeze exactly one off-the-shelf detector replacement, repair its real sparse-label/output contract, and permit one clean development screen only if artifact, offline, cost, and stopping rules are executable | Implemented; RF-DETR Small improved the finite-sequence match count and passed cost, but failed the unchanged recall gate. It is stopped without retry or reserved validation |
| `PUB-DIR-012` | Separate synthetic detector engineering compatibility from scientific priority before spending another target-source development read | Implemented for one D-FINE Small community conversion; engineering cost/determinism passed, but parity and stronger relevant prior did not. No VOST development screen is authorized |
| `PUB-DIR-013` | Stop the off-the-shelf detector tournament unless one candidate has materially stronger, same-protocol and target-relevant evidence than the failed path | Implemented as a no-media M14 decision; D-FINE Medium and RT-DETRv2 Small are not selected. The next work is a target-domain data/training reality gate, not another model preflight |
| `PUB-DIR-014` | Require every target substrate to pass the frozen official-source, localization, terms, acquisition, split, manifest, and bounded-cost gates before selection | Implemented as a no-media M15 decision; HOMAGE, CAD-120, and Watch-n-Patch are all selection-ineligible. The next work proves the target-label oracle and metric semantics without media before choosing a generation strategy |
| `PUB-DIR-015` | Put a complete D1 label-oracle and protected source-group validator before any target media generation or acquisition | Implemented with exact synthetic conformance cases; it validates measurement semantics only. The next work compares generation strategies without installing, downloading, rendering, or training |
| `PUB-DIR-016` | Select at most one D1 generation route only after exact oracle, provenance, split, delivery, storage, and dependency feasibility all pass | Implemented as a no-media M17 decision; the existing project-owned vector renderer is the sole bounded selection. The next work is exactly one tiny deterministic D1 substrate, not training |
| `PUB-DIR-017` | Prove one project-owned image/annotation generation seam before scaling synthetic data or training | Implemented as M18 within a 103,957-byte vector slice. It passes mechanics and reproducibility only; the next work must secure a real transfer oracle before any model experiment |
| `PUB-DIR-018` | Select at most one lawful compact real transfer oracle through current official rights, acquisition, complete-scoring, protected-group, small-target, D1, and cost evidence | Implemented as M19; BOP YCB-V BOP'19 is the sole bounded acquisition choice after the frozen final cost tie-break. Selection is not an accuracy or movement claim |
| `PUB-DIR-019` | Materialize the selected source only after immutable byte identity, full ZIP header safety, synthetic translator conformance, and unchanged D1 rules pass | Implemented as M20; byte identity and synthetic conformance passed, but the frozen single-root archive rule failed before extraction. One per-archive root-mapping repair is proposed without changing scientific gates |

## Open gates and blockers

- Named project, policy, engineering, and data roles remain unassigned.
- The full B0 conformance, recovery, runtime-path, performance, and independent maintainer exercise gates are incomplete.
- The B1 candidate-source seam, generated video/decoder/scheduler slice, conservative binding/relation/query rules, and closed CLI/Streamlit presentation remain unchanged. VISOR and VOST are separate evaluation adapters, not claim-producing sources. VISOR is sparse egocentric kitchen data with active-object rather than exhaustive-scene labels; VOST is consecutive egocentric transformation data whose selected mask is not a COCO class contract. Neither supplies the key→bag→sofa relation, fixed-camera transfer, live-source evidence, or operational capability. The RF-DETR Small development result is rejected; the D-FINE Small result is synthetic engineering evidence only; M14 stops D-FINE Medium and RT-DETRv2 Small from extending the model tournament. M15 selects none of HOMAGE, CAD-120, or Watch-n-Patch. M16 adds only an evaluation oracle. M17 selects only a vector generation strategy, M18 proves its bounded substrate mechanics, and M19 selects only one real transfer-oracle acquisition route. No detector or dataset adapter is promoted into the semantic path.
- No household data class, person, room, camera, credential, endpoint, device, or capability is enrolled.
- Consent, retention, deletion, access, incident, kill-switch, and independent enforcement mechanisms do not exist.

## Next safe action

Run M21 as exactly one per-archive root-mapping infrastructure repair. Freeze base source root `ycbv/`, test source root `test/`, and a destination prefix of `ycbv/` for test members; prove destination collision/escape rejection with committed synthetic archives before reusing the already hash-verified ignored files. All M20 source identities, license expectation, size/time limits, selection order, 0.1–1% target, negative/unknown semantics, zero relation output, and prohibitions remain unchanged. Stop normally on any further mismatch; do not add another repair, substitute data, load a model, train, tune, emit candidates/claims, connect live/private/cloud/action capability, or enable `OPERATE`.
