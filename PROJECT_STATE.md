# Whole Home Agent — Public Repository State

**Checkpoint:** `PUBLIC-B1-M44-001`

**As of:** `2026-09-02 Asia/Taipei`

**Governance:** `PROPOSED — NOT ADOPTED`

**Runtime operation:** `OPERATE DISABLED`

This file is the live checkpoint for the clean public repository. It records current scope and evidence; it grants no household, data, device, account, or operational authority.

## Current direction

- Publish and collaborate on the smallest viable offline B0 semantic replay baseline plus one bounded B1 prerecorded replay.
- Keep B0/B1 inputs limited to synthetic or separately licensed, lawfully reusable public fixtures.
- Keep the product slice limited to the fixed project-generated clip; use the local-only, hash-pinned VISOR sparse-frame set strictly as a non-commercial detector screen behind a separate adapter.
- Use the local-only, hash-pinned VOST consecutive-frame subset strictly as a non-commercial scheduler/cost screen; it is not a product claim source.
- Reuse the implemented B0 `ClaimCandidate` → commit → projection → query path as the only semantic state path for B1.
- Exclude live/private sensing and all physical or external action from the current scope.
- Do not assume a graph, Memory Core, multi-agent runtime, or durable database is necessary.
- Use the representative market synthesis to keep staged compute, deterministic claim
  admission, traceable relations, and a question-first UX while deferring identity,
  audio, proactivity, sensor fusion, device control, and embodiment.
- Keep the deterministic evidence-bound answer as the default presentation. Treat a
  future local model as optional and any future cloud text presenter as local-first
  hybrid, separately blocked by data/egress and runtime authority. An API key is only a
  credential and cannot activate egress.
- Route the closed demo through one local `LocationPresenter` port selected only by the
  composition root. Presenter failure must retain the structured answer and expose no
  exception content; presenter prose is never truth or authority.

## Authority and roles

| Role | Assignment | Current authority |
|---|---|---|
| Repository owner | GitHub account `kr-yep` controls repository settings | Repository administration only; not household or policy authority |
| Project Owner | `UNASSIGNED` | None until explicitly assigned |
| Policy Owner | `UNASSIGNED` | None until explicitly assigned |
| Engineering Lead | `UNASSIGNED` | None until explicitly assigned |
| Data Steward | `UNASSIGNED` | None until explicitly assigned |
| Household roles / affected persons | `UNDEFINED` | No consent or operational authority exists |
| Runtime identity | `NOT CREATED` | No capability or credential exists |

## Document status

| Artifact | Status | Meaning |
|---|---|---|
| `AGENTS.md` | `PROPOSED` | Conservative interim repository instructions |
| `ACTION_POLICY.md` | `PROPOSED — NOT ADOPTED` | All sensing, private-data, egress, device, and physical operation disabled |
| ADR 0001–0005 | `PROPOSED` | Design candidates, not automatically adopted requirements |
| B0 implementation and fixtures | `IMPLEMENTED / VERIFIED IN DECLARED TEST ENVELOPE` | A bounded semantic replay slice exists; its frozen golden semantic hash is unchanged |
| B1 candidate-source seam and run receipt | `IMPLEMENTED / VERIFIED WITH SYNTHETIC CONTRACT TESTS` | Generic finite source contract, provenance types, fail-closed run outcome, and B0 compatibility remain the sole semantic admission path used by the later prerecorded adapter |
| B1 generated-video manifest, PTS decoder, and scheduler | `IMPLEMENTED / VERIFIED ON ONE SYNTHETIC REPLAY` | Hash-pinned allowlisted H.264 source revision 2, exact PTS/time-base decode, and motion-plus-periodic frame selection feed the later bounded perception source; revision 1 remains in Git history |
| B1 perception/tracking/evaluation baseline | `IMPLEMENTED / VERIFIED ON SYNTHETIC REPLAY PLUS BOUNDED PUBLIC SCREEN` | Canonical detections, test-only annotation ceiling, RGB smoke detector, clip-local tracker, fixed quality/cost evaluator, RF-DETR translation seam, and paired torchvision public-data baselines exist; indoor evidence is limited to the separately described VISOR screen |
| VISOR screen v1 and paired public baselines | `IMPLEMENTED / VERIFIED IN A BOUNDED LOCAL ENVELOPE` | Three source-video-split VISOR sequences, strict license/hash manifests, mask-to-box adapter, source-index timing, SSDLite320 and RetinaNet-FPN adapters, one frozen-test lock, and paired quality/cost receipts exist; source data and weights remain ignored and are not distributed |
| Sliced SSDLite validation gate | `IMPLEMENTED / REJECTED BY FROZEN DEVELOPMENT/VALIDATION EVIDENCE` | Four bounded overlapping tiles, original-coordinate translation, wrapper provenance and label-aware NMS were tested without a test-split path; validation quality fell and p95 exceeded the gate, so this candidate is stopped |
| VOST motion screen v1 | `IMPLEMENTED / VERIFIED IN A BOUNDED LOCAL ENVELOPE` | Two upstream-split VOST sequences, range-only acquisition, upstream license/split evidence, source-offset/5 fps replay semantics, development-only scheduler selection, and paired full-frame/scheduled FPN receipts exist; the validation gate passed, while target detection and fixed-camera transfer remain unproven |
| VOST target-tracking screen v1 | `IMPLEMENTED / VALID DEVELOPMENT RUN / REJECTED ON DEVELOPMENT` | A separate bottle-labeled train/validation substrate, mask-to-box target contract, target-event detection coverage, and clip-local tracker gate exist. The development source failed the predeclared quality/tracking gates, so the reserved validation source was not run and movement-candidate work is stopped for this observation path |
| VOST M11 failure localization v1 | `IMPLEMENTED / VALID DEVELOPMENT-ONLY DIAGNOSTIC` | One clean instrumented development pass separated absent/void, missing proposal, confidence-filtered, localization, and matched frames while keeping low-score proposals outside canonical detections. A separate mask-box oracle exposed an independent greedy-IoU association failure; reserved validation remained unloaded |
| VOST M12 RF-DETR Small screen v1 | `IMPLEMENTED / VALID DEVELOPMENT RUN / REJECTED ON DEVELOPMENT` | One immutable RF-DETR Small 1.9.4 adapter path fixed sparse COCO mapping, normalized the product threshold, and executed under a one-attempt offline guard. It improved the finite-sequence match count but missed the unchanged recall gate; the two cost gates passed and reserved validation remained unread |
| M13 D-FINE Small synthetic qualification v1 | `IMPLEMENTED / ENGINEERING COMPATIBLE / NO DEVELOPMENT SCREEN` | One immutable community-converted Safetensors path fixed dense COCO mapping and a closed offline loader, then passed the generated-input determinism and cost checks in one valid real-load attempt. Converted-weight parity and target-source accuracy remain unverified, so VOST was not read and the candidate is not scientifically selected |
| M14 detector scientific-priority reality gate | `IMPLEMENTED / STOP MODEL SWAPPING` | Official primary evidence for exactly D-FINE Medium and RT-DETRv2 Small was compared without model or media loading. Neither supplied a material same-protocol prior over the failed target-screened path; a config coverage omission was recorded fail-closed without changing thresholds |
| M15 target-domain substrate reality gate | `IMPLEMENTED / ZERO ELIGIBLE / PIVOT` | Official primary evidence for exactly HOMAGE, CAD-120, and Watch-n-Patch was checked without media acquisition. None passed every localization, terms, acquisition, split, and bounded-cost requirement, so no dataset was selected and no acquisition or training was authorized |
| M16 target-label-oracle feasibility gate | `IMPLEMENTED / BOUNDED PASS` | A no-media D1 scope layer now validates complete scored/unknown frame accounting, visibility, persistent identity, reference transitions, and protected source groups before reusing the existing quality evaluator. All exact synthetic metric/rejection cases passed; this establishes no media/model/transfer quality |
| M17 generation-strategy reality gate | `IMPLEMENTED / ONE BOUNDED SELECTION` | Exactly the existing vector renderer, Blender headless 3D, and Kubric licensed-asset routes were compared without installation, download, or generation. Only the existing project-owned route passed every frozen feasibility gate; selection authorizes one tiny M18 D1 slice, not training or a transfer claim |
| M18 tiny vector D1 slice | `IMPLEMENTED / BOUNDED PASS` | Three project-owned protected groups now contain 18 PNG/annotation pairs plus one exact M16 fixture. Hash manifests, two-run byte identity, six reference transitions, 0.1–1% targets, and unchanged old golden hashes passed; this is substrate-mechanics evidence, not detector-transfer evidence |
| M19 real transfer-oracle reality gate | `IMPLEMENTED / YCB-VIDEO BOP'19 SELECTED` | The frozen no-download AND matrix leaves GMU ineligible because its official dataset route is unavailable. HB and YCB-V pass through BOP; the final cost tie-break selects only the 660 MB YCB-V BOP'19 route for one acquisition/D1 translation slice |
| M20 YCB-V BOP'19 acquisition/translation | `IMPLEMENTED / NORMAL PRE-EXTRACTION STOP` | Both immutable archives matched frozen sizes and hashes. Materialization stopped before extraction because base is rooted at `ycbv/` but test at `test/`, contradicting the frozen single-root rule. No real annotation or image was read |
| M21 per-archive root repair/translation | `IMPLEMENTED / NORMAL ANNOTATION-SELECTION STOP` | The sole mapping repair passed exact archive identity, full header safety, and collision-free namespace checks. The unchanged annotation-only selector found no same-object/same-scene pair satisfying both the small-positive and complete-absent terms; no RGB or persistent D1 output was read or produced |
| M22 YCB-V annotation failure localization | `IMPLEMENTED / PAIRING-SCOPE DIAGNOSIS` | The exact 900-frame scope contains 81 frozen-bucket positives and 14,775 safe negatives but zero same-object/same-scene paired cells. Cross-scene pairing remains unadopted pending a no-data/no-model validity gate |
| M23 cross-scene transfer-oracle validity | `IMPLEMENTED / ONE BOUNDED SELECTION` | Official BOP/COCO implementations support category aggregation over images from multiple scenes; M16 preserves complete-frame, unknown, provenance, and protected-group semantics. Cross-scene pairing is selected only for one test-only at-most-18-frame diagnostic oracle, not training, movement, relation, or whole-home evidence |
| M24 minimal cross-scene YCB-V D1 | `IMPLEMENTED / LOCAL MATERIALIZATION PASS WITH METRIC GAP` | One positive and one complete class-absent negative materialized as two separate test sequences with deterministic ignored output. The positive passes the frozen visible-pixel area rule but is large under M16 bbox-area sizing, so no small-bbox detector experiment is authorized |
| M25 YCB-V small-bbox alignment | `IMPLEMENTED / ANNOTATION-ONLY DUAL-AREA SELECTION` | Exactly 2 of 900 reference frames pass both the visible-pixel and M16 bbox-area 0.1–1% predicates. The fixed pair selects object 4 at scene 50/image 722 and a scene 48/image 1 complete-absent negative; only one exact replacement materialization is authorized |
| M26 exact dual-area replacement D1 | `IMPLEMENTED / LOCAL METRIC-ALIGNED MATERIALIZATION PASS` | The exact M25 pair materialized twice with byte-identical ignored outputs. M16 counts one small-bbox target, one complete negative, zero tiny/large targets, and zero transitions; no model or gain experiment is authorized |
| M27 primary demo/evaluation decision | `IMPLEMENTED / EXISTING SYNTHETIC E2E SELECTED` | The packaged project-owned key→bag→sofa replay passes all seven primary-demo gates. M26 remains optional mechanical smoke; future gain requires separate protected-group development and untouched-test authority |
| M28 primary demo acceptance | `IMPLEMENTED / NORMAL STOP WITH PRESENTATION HARDENING` | Direct and compact-CLI replays agreed offline on the scoped answer and semantic document. Two synthetic-metric presentation gaps were hardened; a frozen event-frame versus evidence-window mismatch remains for one separately contracted M29 retry |
| M29 primary demo acceptance retry | `IMPLEMENTED / NORMAL STOP / NO SECOND RETRY` | The sole committed retry validates event-label, evidence-window, and confirmation-frame meanings plus every non-answer check. It stops only because the top-level answer omits queried `subject_id`; the relation path still traces `key` |
| M30 answer-subject identity decision | `IMPLEMENTED / CANONICAL ANSWERTRACE SELECTED` | Repository-only scoring selects required query-scope `AnswerTrace.subject_id` at 8/8 gates. Public-DTO-only and no-change options are ineligible; M31 may implement only the additive canonical field and two serializers |
| M31 answer-subject implementation | `IMPLEMENTED / SEMANTIC CHECKS PASS / CONTRACT NORMAL STOP` | Required immutable `subject_id`, sole-constructor copy, and B0/B1 serialization pass all six statuses with unchanged B0 semantic hash. The Goal stops because full regression reads the pinned synthetic clip while the contract also forbade all media reads |
| M32 verification-fixture boundary | `IMPLEMENTED / EXPLICIT PROFILES SELECTED` | Repository-only scoring selects static-contract, complete-regression, and ad-hoc profiles. Only complete regression may read the exact manifest-backed project-owned D0 synthetic fixture, as regression evidence only; M31 remains a normal stop |
| M33 teammate clean-install drill | `IMPLEMENTED / NORMAL STOP BEFORE INSTALL` | Exact public clone succeeded in 1.608 s, but the outer preflight rejected uv 0.11.24 because Windows build metadata followed the semantic version. No sync/demo ran; cleanup passed; clean-install usability remains unverified |
| M34 teammate drill infrastructure retry | `IMPLEMENTED / MECHANICAL PATH PASS / CONTRACT NORMAL STOP` | Clean clone, cold-cache locked install, offline answer/trace, timing, network, and cleanup passed. Windows CRLF checkout changed the text lock raw hash while its Git blob stayed exact; no retry remains |
| M35 versioned-text identity decision | `IMPLEMENTED / GIT BLOB SELECTED` | Repository-only scoring selects exact HEAD blob SHA-256 plus exact revision and clean worktree. Raw checkout hash is diagnostic; canonical-LF fallback is rejected; M34 remains STOP |
| M36 checker Git-blob identity hardening | `IMPLEMENTED / FOCUSED, REGRESSION, AUDIT, AND PUBLIC CI PASS` | Teammate checker now uses committed Git-blob identity and separate worktree diagnostics. LF/CRLF, tamper, wrong-hash, and non-Git tests pass; all 382 regression tests, the 295-file public audit, and six public CI jobs pass; no acceptance rerun occurred |
| M37 teammate handoff runbook | `IMPLEMENTED / LOCAL, AUDIT, AND PUBLIC CI PASS` | Exact Windows and macOS/Linux procedures, receipt interpretation, bounded troubleshooting, guarded cleanup, presentation fallback, and result template exist. All 393 local tests, the 300-file public audit, and six follow-up public CI jobs pass. First CI's depth-1 test-only failure is retained; no real teammate receipt or acceptance rerun exists |
| M38 market synthesis and text-context preview | `IMPLEMENTED / LOCAL OFFLINE VERIFICATION` | Representative primary sources are reduced to an explicit adopt/defer/reject product cut. A pure allowlist projection exposes only answer and relation facts in the closed Streamlit demo. It has no provider, network, credential, prompt input, memory reader, or action handle; cloud egress remains prohibited |
| M39 language-presentation boundary | `IMPLEMENTED / REPOSITORY DECISION / NO PROVIDER` | Ten fatal gates select deterministic local default plus separately authorized cloud replaceability. Every M38 field is classified; credential, retention, telemetry, network-failure, and fallback boundaries are explicit. This does not adopt policy, configure a key, or authorize a request |
| M40 deterministic local presentation | `IMPLEMENTED / LOCAL VERIFICATION / NO MODEL` | One narrow port validates the exact M38 context and bounded output, then emits a sanitized receipt. The composition root selects only a deterministic local presenter; structured answer/evidence survive fallback and no provider surface exists |
| M41 release-candidate packaging | `IMPLEMENTED / NORMAL PRE-ARTIFACT STOP` | The sole exact-revision offline build attempt stopped during default `uv` cache initialization with Windows error 183. No sdist, wheel, environment, install, or demo was produced; cleanup passed and no retry or push is authorized |
| M42 explicit uv cache preflight | `IMPLEMENTED / NORMAL PRE-PROBE STOP` | The exact repository-local path was accepted and reported by `uv cache dir`, but uv did not create it. The frozen uv-initialization condition failed before the write probe; no build, cache, retry, or push is authorized |
| M43 caller-created uv cache | `IMPLEMENTED / BOUNDED NO-BUILD PASS` | A closed preflight created and probed the exact ignored cache, uv confirmed that existing path, and non-recursive cleanup proved it stayed empty. This authorizes only a separate explicit-cache packaging gate, not a build or push |
| M44 explicit-cache release-candidate packaging | `IMPLEMENTED / NORMAL STOP AFTER SDIST, BEFORE WHEEL` | The sole offline attempt seeded the exact cache and started the build, but implicit CP950 decoding broke runner receipt assembly. Read-only forensics found one partial sdist, no wheel, and a missing required `uv.lock`; install/demo never started and no retry or push is authorized |
| B1 binding/relation/query slice | `IMPLEMENTED / VERIFIED ON ONE SYNTHETIC REPLAY` | One-instance binding, conservative containment/zone assertion and retraction rules, typed abstentions, estimated candidates through the unchanged committer, relation evaluation, and evidence-traceable `key → bag → sofa` query exist; no real indoor transfer evidence exists |
| B1 CLI and Streamlit presentation | `IMPLEMENTED / VERIFIED ON ONE SYNTHETIC REPLAY` | A closed composition/presentation boundary exposes the fixed clip, scoped answer, evidence, abstentions, metrics, diagnostics, and receipt; it accepts no upload, camera, arbitrary path, free-form query, credential, or action handle |
| Python package distribution | `IMPLEMENTED / VERIFIED BY LOCAL CLEAN INSTALL` | The wheel contains only the fixed D0 replay, its configs, and generator provenance; a fresh Python 3.12 environment ran the CLI from outside the checkout |
| `docs/b0-b1-architecture-plan.md` | `PROPOSED — NOT ADOPTED` | Smallest B0 → B1 boundary, quality scenarios, gates, and deferred complexity |
| ADR 0005 and `docs/evaluation/visor-screen-v1.md` | `PROPOSED DESIGN / VERIFIED BOUNDED EVIDENCE` | Public sparse-frame evaluation design and the exact Reality Gate findings; neither grants sensing, product, or policy authority |
| B0 → B1 Archify diagrams | `VERIFIED RENDERING OF THE IMPLEMENTED BOUNDED SLICE` | 9/9 showcase and visual delivery passed; rendering evidence does not adopt governance or expand the implementation envelope |
| MIT `LICENSE` | `SELECTED FOR PUBLIC RELEASE` | Applies to original repository code and documentation |

## Evidence envelope

- The public export contains the B0 package, synthetic semantic fixtures, one project-generated CC0 prerecorded clip with manifests/annotations, B1 adapters, public asset/hash configs, tests, presentation code, architecture/evaluation documents, and proposed governance. It does not contain VISOR/VOST source bytes or model weights.
- It intentionally excludes prior private Git history, coursework/competition experiments, third-party datasets, household/private media, model weights, run outputs, local databases, environment files, and credentials.
- Existing tests exercise semantic replay, provenance, idempotency, conflict/unknown handling, malformed input, boundary restrictions, media allowlisting and timing, detector/tracker/evaluator contracts, conservative relation inference, partial-source failure without a queryable session, and the fixed CLI/Streamlit presentation.
- Local M8 verification used Python 3.12.13 with bytecode writes disabled: `80/80` unit, contract, integration, audit, CLI, Streamlit, VISOR/hash/source-index, torchvision-translation, and sliced-wrapper tests passed. The frozen B0 semantic hash remains `226d30a5b826720d607d0b9a29bf3dfb9f5429eeedbbd70ffd1ff23c21233c8f`.
- Local M9 verification used the locked project Python 3.12.13 environment with bytecode writes disabled: `84/84` unit, contract, integration, audit, CLI, Streamlit, VISOR, VOST manifest/hash/split/source-index, motion-coverage, torchvision-translation, and sliced-wrapper tests passed. The staged public-release audit scanned 109 files / 218 index-and-worktree snapshots with zero violations and `operate_enabled: false`.
- Local M10 verification used Python 3.12.13, torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, and the RTX 4070 Laptop GPU. `85/85` tests passed before the clean development run. The staged implementation audit scanned 112 files / 224 index-and-worktree snapshots with zero violations and `operate_enabled: false`.
- Local M11 implementation verification used Python 3.12.13. `90/90` unit, contract, integration, audit, CLI, Streamlit, VOST, diagnostic-seam, mutually-exclusive-classification, and detector-priority tests passed before and after the clean diagnostic run. The final staged public audit scanned 119 files / 238 index-and-worktree snapshots with zero violations and `operate_enabled: false`; the offline source/wheel build also passed. The diagnostic used torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, and the RTX 4070 Laptop GPU.
- Local M12 verification used Python 3.12.13. `96/96` unit, contract, integration, audit, CLI, Streamlit, VOST, sparse-COCO, strict-gate, offline-guard, and one-attempt tests passed before the implementation commit and again after the result documentation. The final staged public audit scanned 124 files / 248 index-and-worktree snapshots with zero violations and `operate_enabled: false`. The candidate used torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, RF-DETR `1.9.4`, CUDA `12.8`, cuDNN `91900`, driver `610.74`, and the RTX 4070 Laptop GPU; the current `uv.lock` does not reproduce that CUDA package set.
- Local M13 pre-attempt verification used Python 3.12.13 and passed `110/110` unit, contract, integration, audit, CLI, Streamlit, VOST, D-FINE fake-output, dense-COCO, import-isolation, generated-fixture, offline-guard, p95, and one-attempt tests; the post-result suite passed `111/111`. The final staged public audit scanned 131 files / 262 index-and-worktree snapshots with zero violations and `operate_enabled: false`. The sole valid real-load attempt used torch `2.11.0+cu128`, torchvision `0.26.0+cu128`, Transformers `5.16.1`, Safetensors `0.8.0`, CUDA `12.8`, cuDNN `91900`, and the RTX 4070 Laptop GPU; the current `uv.lock` is explicitly recorded as not reproducing that GPU package set.
- Local M14 verification used the locked Python 3.12.13 environment and passed `117/117` unit, contract, integration, audit, CLI, Streamlit, VOST, detector-adapter, no-media priority, scope-gap, and boundary tests. The final staged public audit scanned 137 files / 274 index-and-worktree snapshots with zero violations and `operate_enabled: false`. M14 performed no model or media load and added no runtime dependency or adapter.
- M15 used only official dataset pages, papers, terms, author pages, and endpoint metadata. HOMAGE failed the per-frame localization requirement because its scene graph protocol samples 3 or 5 frames per atomic interval and covers interacted objects; CAD-120's accessible official use terms and current acquisition path were not established; Watch-n-Patch did not establish per-frame reference object boxes and likewise lacked verified current official terms/acquisition. `UNKNOWN` remained distinct from factual `FAIL`, but both were selection-ineligible under the frozen AND gate. The locked Python 3.12.13 environment passed `126/126` tests, and the staged public audit scanned 143 files / 286 index-and-worktree snapshots with zero violations and `operate_enabled: false`. The bounded result is zero eligible and a pivot to a no-media D1 target-label-oracle feasibility gate; no media/archive/model load, training, source read, movement candidate, claim, or operational capability was added.
- M16 introduced one pure D1 target-oracle boundary and one hash-pinned `D0_SYNTHETIC` semantic fixture while reusing the existing canonical box/detection/quality calculator. Perfect, empty, duplicate, wrong-class, bad-localization, explicit-negative-frame, ordering, unknown, identity-conflict, and every protected split-group case produced the frozen exact result or failure code. On clean implementation revision `7c2f50425590de39cdb2d62ebb37d0ed9c529b30`, Python 3.12.13 passed `138/138` tests and the public audit scanned 147 files / 294 snapshots with zero violations. The final staged result suite passed `140/140`, and the audit scanned 151 files / 302 snapshots with zero violations and `operate_enabled: false`. No image/video was generated or downloaded; no model, tracker, training, private/reserved source, movement candidate, semantic claim, or operation was introduced.
- M17 compared the three frozen generation strategies using repository evidence and official tool/asset capability, license, and distribution sources only. The existing 2D renderer was uniquely eligible: it already owns its artwork, represents stable key/bag/sofa identities and two transitions, renders the 28×12 key at about 0.1458% of a 640×360 frame, and records reproducibility hashes. Blender and Kubric document richer capabilities but retained selection-ineligible integration/delivery unknowns; Kubric's canonical network/container/cloud-asset route also conflicts with the frozen no-cloud gate. On clean result revision `1e3e4a1ae12e9f8f2156148620d6b889770a2842`, Python 3.12.13 passed `149/149` tests and the public audit scanned 157 files / 314 snapshots with zero violations and `operate_enabled: false`. No dependency/tool was installed, no asset/media was downloaded or generated, no model was loaded, and no training or operational capability was introduced.
- M18 generated exactly three project-owned protected source groups with 18 640×360 PNG/annotation pairs and one exact M16 fixture. Each split contains visible 0.1458%-area keys, truncated/occluded/absent/unknown states, an explicit scored negative, and containment/location reference transitions. Two clean Windows generations matched every committed output and manifest byte; Linux CI later showed that PNG bytes differ across platforms while two Linux generations remain internally identical. Cross-platform PNG byte identity is therefore not established. Total non-manifest output was 103,957 bytes and the old B1 generator/media/annotations/manifest hashes remained unchanged. On clean implementation revision `a7ea0b8582558c293307e727c703bcb059c6613a`, Python 3.12.13 passed `161/161` tests and the public audit scanned 198 files / 396 snapshots with zero violations; the pre-CI result suite passed `164/164` and audited 201 files / 402 snapshots with zero violations and `operate_enabled: false`. No external asset, new dependency, model, training, private/reserved source, claim, or operational capability was used.
- M19 checked exactly GMU Kitchens, HomebrewedDB, and YCB-Video using current official author/institution papers, dataset pages, licenses, archive listings, BOP format/task documentation, and toolkit parameters. GMU's author-cited data URL now redirects to the department home page, so current terms, acquisition, and safe complete scoring are not established. HB and YCB-V both pass the frozen feasibility gates through BOP. The first two tie-breaks remain tied; YCB-V wins the final cost tie-break because its annotated BOP'19 route is displayed as 660 MB versus 2.35 GB for HB's smallest public annotated route. On clean result revision `380ebf18fbb463af28ba975ddcceaff3cbfeccef`, Python 3.12.13 passed `173/173` tests; the final staged public audit scanned 207 files / 414 snapshots with zero violations and `operate_enabled: false`. No archive, media, annotation, model, or reserved local source was read; no training, candidate, claim, or operation occurred. The 0.1–1% target is an executable post-acquisition pixel-count check, not a preverified observation.
- M20 froze its contract at `2a69db3` and its synthetic translator/archive guard at `769c2ae` before downloading any real data. Both selected archives from immutable BOP revision `5c2c4aa229800355648cd268040aa814f8dc94f0` matched the exact frozen byte counts and SHA-256 values. Header preflight then found `ycbv/` in the base archive and `test/` in the test archive, so the frozen single-root requirement stopped the run before extraction. Zero real annotation/image files were read, and no D1 slice, model, prediction, training, candidate, claim, relation, or operation was produced. On clean result revision `ff4368eba2651f1a7aa182039c40197b1bbdf738`, Python 3.12.13 passed `188/188` tests and the public audit scanned 218 files / 436 snapshots with zero violations and `operate_enabled: false`. The local archives remain under the ignored `data/external/` root for one separately frozen infrastructure repair.
- M21 froze the only allowed archive mapping repair at `07f4983` and committed its no-network selective materializer plus synthetic safety suite at `5164634` before real archive reuse. Both source hashes, all 10,092 ZIP headers, 671,106,741 uncompressed bytes, and the combined mapped namespace passed. The run transiently read 37 annotation JSON files covering 4,123 target entries / 900 frames in scenes 48–59, then returned `NO_FROZEN_SLICE` because the combined same-object/same-scene small-positive plus complete-absent predicate had no match. The staging tree was removed; zero RGB, persistent extracted files, D1 outputs, models, predictions, training, candidates, claims, relations, or operations occurred. M21 does not establish which predicate term failed independently. On clean result revision `5da866ce90b57baf77bb22d1eb040fbab5e20bad`, Python 3.12.13 passed `203/203` tests and the public audit scanned 222 files / 444 snapshots with zero violations and `operate_enabled: false`.
- M22 froze its diagnostic at `41e0247` and committed the shared parser/pure diagnostic at `11f083e` before annotation reread. The first real attempt exposed an implementation-only tuple-order bug and made no source-validity finding; `c169693` added semantic filename mapping plus a regression test before one retry. The retry read the same 37 JSON members directly from the pinned ZIPs and found 81 small-positive frames (object IDs 4, 17, and 18), 14,775 safe negative object/frame cases across all 21 classes, and zero paired object/scene cells. No extraction, media, D1, model, prediction, training, claim, relation, or operation occurred. This diagnoses the M21 pairing-scope failure but does not adopt cross-scene evaluation. On clean result revision `ed4d63baacfd40208db1648c8d0f30970a2ffc29`, Python 3.12.13 passed `220/220` tests and the public audit scanned 228 files / 456 snapshots with zero violations and `operate_enabled: false`.
- M23 froze its five-gate no-data/no-model contract at `3205d2c`. Current official BOP code merges per-scene target annotations/results before one COCO evaluation over all selected image IDs, while official COCO code aggregates per-category TP/FP evidence across those images. The local M16 executable contract retains every scored-frame denominator, rejects unknown-frame predictions and protected-group split leakage, and preserves source identities. All five gates pass only for a test-only diagnostic sentinel capped at 18 frames. No source/media reread, download, D1 output, model, prediction, training, tuning, candidate, claim, relation, or operation occurred. On clean result revision `799af167c9568d1293e149368f7831a745ad947a`, Python 3.12.13 passed `228/228` tests and the public audit scanned 232 files / 464 snapshots with zero violations and `operate_enabled: false`.
- M24 froze its materialization contract at `4e0be76ddd64def94f47b4bbb733dd200671ba42`, committed the synthetic implementation at `5a8c42f`, and fixed direct startup at `5f15d0c` before one real run. Both archive identities, all 10,092 headers / 671,106,741 uncompressed bytes, mapped namespace, and the 900-frame annotation scope passed. Two clean runs produced the same four ignored files (1,041,911 bytes). Object 4 was selected at scene 50/image 620 with a scene 48/image 1 class-absent negative; they remain two M16 test sequences with no cross-scene physical identity or transition. The visible-pixel fraction is 0.5107%, but bbox area is 3.043%, yielding zero M16 small-bbox targets. This passes the frozen materialization mechanics but blocks detector work pending M25. No download, model, prediction, training, tuning, claim, relation, or operation occurred. On clean result revision `149aa34aa921b9396032164d5db38a16defa6cfa`, Python 3.12.13 passed `243/243` tests and the public audit scanned 237 files / 474 snapshots with zero violations and `operate_enabled: false`.
- M25 froze the dual-area predicate at `73af8ffc73a74c9f69b3b7b9fda9e96028592f0a` and committed its pure synthetic implementation at `5bbf39efc43bd3f5ab944f369b0016acc25b6d8b` before one annotation-only reread. Both pinned archives and the same 4,123 entries / 900 frames / 37 JSON members passed. Two in-memory pure runs were byte-identical. Of 81 visible-pixel-small frames, only 2 also have an M16-small visible bbox: object 4 at scene 50/image 722 and object 18 at scene 59/image 164. The fixed order selects object 4 with scene 48/image 1 as its complete class-absent negative. This proves dataset suitability for one exact replacement materialization only; one positive and one negative cannot establish detector gain, transfer, movement, or product value. No RGB, model, prediction, training, tuning, claim, relation, or operation occurred. On clean result revision `e9185da4132265e2bccba8fd4c67cdd965b5e6bb`, Python 3.12.13 passed `262/262` tests, the public audit scanned 242 files / 484 snapshots with zero violations and `operate_enabled: false`, and public CI run `33512733734` succeeded.
- M26 froze the exact pair at `f475ad0b077d8838ad0bab4680ad72b2a0010ffc`. Synthetic conformance exposed a new use-class outside M16's closed allowlist, so before any source read the contract reused the existing test-only class at `ac4b0a649179a4cbc721d61439e11f486a058d6f` without changing frames or thresholds. The implementation at `75db4457bcd466f5d41dc3cbffeb727e035888d7` then made one source pass: both archives, all 10,092 headers / 671,106,741 uncompressed bytes, namespace, 900-frame annotation scope, and exact M25 pair passed. Only two RGB members were read. Two clean ignored outputs matched byte-for-byte by relative record and retained 4 files / 1,066,049 bytes. M16 reports tiny/small/large target counts 0/1/0, one negative, and zero transitions. No model, prediction, training, tuning, candidate, claim, relation, or operation occurred. Python 3.12.13 passed `276/276` tests and the public audit scanned 247 files / 494 snapshots with zero violations and `operate_enabled: false`. Result CI `33514137173` exposed only a Windows-hardcoded test interpreter path; revision `7f1f669ef295d959919b80e6389cd0637ea64cc1` replaced it with the active interpreter without rereading source, and CI `33514451966` passed.
- M27 froze three primary-demo options, seven fatal AND gates, and three non-conflatable evaluation lanes at `f02555accafd0909756bed6ea0802b535590576b`. Repository-only scoring selects the existing project-owned synthetic end-to-end replay at 7/7 PASS. M26 fails four primary-demo gates and remains optional smoke; the new-model-first option has seven UNKNOWNs and is deferred. The hostile review keeps synthetic, perfect-fixture, no-live, tiny-oracle, and fixed-query limits visible. No source/media/network/model read, prediction, training, tuning, candidate, claim, relation, or operation occurred. On clean result revision `052079d10fb69a35f7b8ddf8771e152ce684aafe`, Python 3.12.13 passed `286/286` tests, the public audit scanned 252 files / 504 snapshots with zero violations and `operate_enabled: false`, and public CI `33515503351` succeeded.
- M28 froze its acceptance at `01b6bccba1475d956b7f72132fbf00318e6a7b48`. An accidental first checker invocation from that contract revision ran with the checker still uncommitted; this provenance is retained. The direct and compact-CLI paths made zero socket attempts, used the exact pinned project-owned 80-frame source, agreed on semantic SHA-256 `18233bb9d86cc6ff3cdd3a8b7179ac8019e6dd90de790b4e215f49fcfa11936e`, and returned the scoped estimated `FOUND sofa` answer. The frozen check stopped on three assertions: two synthetic-metric presentation gaps, hardened without semantic change at `2d8bf868fa4c3bb2ee1d1332535b882bd0d45dbe`, and one unresolved trace mismatch because source event-label frames 35/65 are not engine evidence windows 33→37/66→68. Python 3.12.13 passed `299/299` tests and the public audit scanned 258 files / 516 snapshots with zero violations and `operate_enabled: false`; public CI run `33516926134` succeeded for result revision `7d940c7fe8e4045746760d473ffe9923d8fac823`. No second M28 acceptance attempt, third-party/private media read, model load, training, tuning, claim/relation change, live/cloud/action connection, or `OPERATE` enablement occurred.
- M29 froze its one-retry contract at `08ef074c310a971c14bb82947037a8a4f130fc5f` and committed the closed checker at `714a0fdcc6f8ba3db6ce00bd471754da4dd6d760` before execution. The clean-worktree retry made two internal offline replays with zero socket attempts and equal semantic SHA-256 `18233bb9d86cc6ff3cdd3a8b7179ac8019e6dd90de790b4e215f49fcfa11936e`. Source event labels 35/65, engine evidence windows 33→37/66→68, and confirmations 37/68 all pass, as do source, governance, warnings, diagnostics, receipt, presentation, judge card, and determinism. The sole failure is `SCOPED_ANSWER`: the top-level answer omits the expected `subject_id: key`, although the two-step relation path contains `key`. Python 3.12.13 passed `314/314` tests and the public audit scanned 262 files / 524 snapshots with zero violations and `operate_enabled: false`; public CI run `33518077833` succeeded for result revision `e68f1cbe6667c49ad85c6040787f31f134288ff0`. M29 authorizes no second retry. No third-party/private media, new model, prediction experiment, training, tuning, semantic/presentation change, live/cloud/action connection, or operation occurred.
- M30 froze three candidates and eight fatal gates at `631ec6056153bd6cb6dbe96a099aece5bbb62b35`, then scored only hash-pinned repository evidence. `QueryRequest` has the identity, `AnswerTrace` omits it, the sole constructor already receives the request for every status, both B0/B1 serializers omit it, and non-FOUND paths can be empty. Required canonical `AnswerTrace.subject_id` passes 8/8; public-DTO-only fails canonical/parity/completeness/no-drift gates, and no-change fails identity/source/parity/non-FOUND/clarity gates. Compatibility risk remains visible: serialized output is additive, strict consumers are unknown, and manual/positional construction may need updates. Python 3.12.13 passed `324/324` tests and the public audit scanned 267 files / 534 snapshots with zero violations and `operate_enabled: false`; public CI run `33519027949` succeeded for result revision `d346f8b0c76d6335d003ad758488e72a56cf882b`. No media/model read, demo/checker execution, code/schema/presentation change, prediction/training/tuning, M29 retry, operational connection, or `OPERATE` enablement occurred.
- M31 froze its bounded contract at `12df5289f1b8c1ddd76d59f6afe4d85da59f41c7` and implemented the four-file additive change at `5c08ea65154cc607c510c5eb65b16657632fd769`. Required immutable `AnswerTrace.subject_id` comes only from `QueryRequest`; both B0/B1 serializers expose it; all six statuses preserve it, including empty-path non-FOUND results; existing fields, claim IDs, projection frontier, and B0 semantic SHA-256 `226d30a5b826720d607d0b9a29bf3dfb9f5429eeedbbd70ffd1ff23c21233c8f` remain unchanged. All `338/338` tests pass, and the public audit scanned 272 files / 544 snapshots with zero violations and `operate_enabled: false`; public CI run `33520669687` succeeded for result/handoff revision `ad8e78dbbb24315ecf16926b2dec2209343881c4`. The Goal nevertheless normally stops because its complete-suite requirement caused the pinned project-owned D0 synthetic clip to be read while its literal contract also required zero media/demo execution. No third-party/private media, model, M29 checker/retry, query/claim/relation/epistemic change, or operational connection occurred.
- M32 froze three candidates and eight fatal gates at `a28ae6c09e07369ad35976d595b301bcf2c59518`. Repository text and metadata show that the pinned CI workflow, public-demo regression, fixture manifest, and release audit intentionally form a closed hash-checked D0 conformance path. Explicit profiles pass 8/8; universal zero-media verification fails profile/receipt gates, while broad D0 allowance fails six identity/purpose/extension gates. Scoring executed no tests/demo/checker/audit, read no media/model, changed no production boundary, and did not rewrite M31. After selection, all six complete-regression jobs, including D0, closed-demo, and release-audit jobs, succeeded in public CI run `33521751091` for revision `5ea23d81f762d060a2cc8f9b7e22ae686d595a0f`.
- M33 froze its one-attempt contract/checker at `dc2b6fd2a51e8ea09ef199d7f4076ac74c9183b2`, whose public CI run `33523229011` succeeded. The public clone then completed at that exact clean revision in 1,607.584 ms. Install preflight observed semantic uv version 0.11.24 plus Windows build metadata, but the outer orchestration compared the whole string and stopped before `uv sync`. No dependency compatibility, demo, decoder, checker, or output claim was evaluated. The clone and disposable empty cache were removed. This is only `INSTALL / UV_VERSION_OUTPUT_FORMAT`, not a repository, dependency, or demo failure. Python 3.12.13 passed `9/9` focused result tests, the staged public audit scanned 282 files / 564 snapshots with zero violations and `operate_enabled: false`, and result CI run `33523965487` succeeded.
- M34 froze exactly one parser-only retry at `8db1745b37484cee20b3bc76a09a25b84d4ee2c1`, whose public CI run `33524517567` succeeded. The corrected preflight parsed uv 0.11.24, then an exact clean clone and new empty cache installed 40 locked packages in 7,059.658 ms. The unchanged checker ran offline in 21,754.661 ms with zero socket attempts and returned the exact scoped key→bag→sofa answer, two claims, relation path, complete receipt, and semantic SHA-256 `23d57ee4bb7c612c752f61369d5b18450532d24283bacc59b7683b0a2d35d322`. It normally stopped only because Windows `core.autocrlf=true` converted the 571,954-byte LF `uv.lock` Git blob (`f2d363…`) into 574,709 CRLF working-tree bytes (`2cd58c…`); `uv sync --frozen` and the manifest both passed. Clone/cache cleanup ultimately passed, with elevation required for one cache ACL. Python 3.12.13 passed `9/9` focused tests, the staged public audit scanned 286 files / 572 snapshots with zero violations and `operate_enabled: false`, and result CI run `33525316891` succeeded. No retry remains.
- M35 froze three candidates and eight fatal gates at `cf90586078012930c5683f3ec1b6f4adb8d62aa0`, then used repository text and M34 byte evidence only. Exact `HEAD:uv.lock` Git-blob SHA-256 plus exact revision and clean worktree passes 8/8. Canonical-LF worktree hashing fails version-authority and explicit-environment gates; raw working-tree hashing fails checkout-stability, version-authority, and epistemic-clarity gates. The selection permits only separately frozen M36 checker/test hardening, with no `.gitattributes`, lock, fixture, product, demo, acceptance, or M34 status change. Python 3.12.13 passed `7/7` focused tests, the staged public audit scanned 291 files / 582 snapshots with zero violations and `operate_enabled: false`, and result CI run `33526177365` succeeded.
- M36 froze its checker-only contract at `2cb2e9ae339ce6368a554bbc4e72e15969f385de`, implemented Git-blob identity at `82378fe40a635c019a66ae481ab26848b35035fc`, and preserved prior checker hashes as historical literals at `d7cab829cf67dd57eeec6b395183f7294648e11b`. Exact `HEAD:uv.lock` blob identity, exact revision, and clean worktree remain fatal checks; raw checkout hash and representation match are explicit diagnostics. Temporary Git repositories establish LF/CRLF equivalence at the blob boundary, dirty tamper, wrong-hash rejection, and non-Git failure. Python 3.12.13 passed `12/12` focused tests and `382/382` complete-regression tests; the public audit scanned 295 files / 590 snapshots with zero violations and `operate_enabled: false`; all six jobs in public CI run `33527744800` succeeded. No clone/install/demo/checker acceptance or M34 retry occurred.
- M37 froze its document-only contract at `3310e3ab05a51acab6a791a9a718f7bdc507eca2`, normalized it at `9c5caddcec8310ddb485f9edcfa87f7e8387131c`, and added the runbook at `aaa48549eea700a4fdf215b76fec8342e55043af`. The handoff pins M36 revision `f16a0a4f99ac97dce16430b70568a3f47613cc0d`, Python 3.12, uv 0.11.24, exact checker/lock identities, both shell procedures, explicit receipt meanings, failure actions, guarded cleanup, 90-second presentation, CLI fallback, and sanitized reporting. Python 3.12.13 passed `11/11` focused and `393/393` complete-regression tests; the public audit scanned 300 files / 600 snapshots with zero violations and `operate_enabled: false`. Public CI run `33529559775` passed both specialized jobs but failed Python 3.11–3.14 because the new test requested a historical commit unavailable in the default depth-1 checkout. Test-only revision `554757de905bae1a492a47f87b13e1827ae1e2d6` checks the same retained lock identity at `HEAD`; local `393/393` passes and follow-up public CI run `33530452074` succeeded across all six jobs. No clone/install/demo/checker acceptance occurred, and no teammate/platform success is claimed.
- M38 adds a representative market synthesis, ADR 0020, one pure allowlist projection,
  and one Streamlit preview over the unchanged fixed synthetic replay. Python 3.12.13
  passed `399/399` complete-regression tests. The public audit scanned 304 files / 608
  index-and-worktree snapshots with zero violations and `operate_enabled: false`.
  This is local implementation evidence only: no provider, request, credential, model,
  household data, egress, claim-semantic change, action, or operational activation exists.
- M39 froze three architecture candidates and ten fatal gates at
  `f5bdbb30e4e9f69fb3ab2b101baa6ab457899350`. Local default with separately
  authorized cloud replaceability passes 10/10. API-key autostart fails all ten gates;
  permanent local-only fails the requested replaceability gate. The decision classifies
  all ten M38 field paths and separates training, abuse-monitoring, and application-state
  retention claims. Python 3.12.13 passed `12/12` focused and `411/411` complete tests;
  the public audit scanned 309 files / 618 snapshots with zero violations and
  `operate_enabled: false`. This is local-branch evidence only: no provider, dependency,
  key, endpoint, request, model, private data, policy adoption, action, or push occurred.
- M40 froze the local implementation at
  `99d785e1b77f3ad36b9d178c8c8f13a4690d0518` and implemented it at
  `9d0fc81e47f0077e5dce6e7a244d866826506d53`. One port and deterministic
  presenter accept only the exact M38 context, validate bounded prose, and return an
  explicit receipt or sanitized fallback. The previous temporal put/move wording was
  removed because that sequence is not present in the presenter context. Python 3.12.13
  passed `35/35` focused and `433/433` complete tests; 38 optional-dependency cases were
  skipped under their existing guards. The public audit scanned 315 files / 630
  snapshots with zero violations and `operate_enabled: false`. No provider, model,
  dependency, key, endpoint, request, private data, action, policy adoption, or push
  occurred.
- M41 froze its no-push packaging gate at
  `12fcbd6bc607c83e608b4668e8a873a7538d5832` and used that same exact revision for
  its sole attempt. Source and committed lock identity passed, but `uv build --offline`
  exited in `58.167 ms` while initializing the default Windows cache path with operating-
  system error 183. The observed path was a directory, so the lower-level cause remains
  unresolved. Zero artifacts were produced; install and demo never started; all
  disposable paths were removed. The offline flag is recorded, but OS-level network
  attempts were not instrumented and are not claimed. Nothing was pushed and `OPERATE`
  remained disabled. Post-result revision
  `f514de3a358fe2fa7c7a4a927c1e9fe39be901a2` passed all 445 local regression tests
  with 39 existing optional-dependency skips; the Git-mode public audit scanned 321
  files / 642 snapshots with zero violations.
- M42 froze its no-build preflight at
  `ba3ab63f1a83630d86a7c45b741a0a1d8e4ad0c3`. Its only uv cache-directory
  command accepted and echoed `.tmp/m42-uv-cache` under offline/no-config/no-download
  flags in `33.830 ms`, but did not create that path. The frozen gate stopped as
  `CACHE_NOT_INITIALIZED` before its write probe. The target never existed, the empty
  parent was removed, and no cache, build, install, demo, product change, retry, or push
  occurred. This does not establish writability, default-cache root cause, or zero
  OS-level network attempts. Result revision
  `a1c1de835a543747f006d01e8f8bc35de1022b4a` passed all 458 local regression tests
  with 39 existing optional-dependency skips; the Git-mode public audit scanned 327
  files / 654 snapshots with zero violations.
- M43 froze its caller-created-cache contract at
  `9464c0c430e3e9251565da3ad4b6f1e9ae44c50b`. A direct-script import failure
  occurred before any target/probe/uv attempt; checker-only revision
  `b86e2c0db48f82ee87348731cce49cf1658906c1` fixed that launch boundary and added
  an outside-repository `--help` regression. The sole cache attempt then exclusively
  created, fsynced, read back, and deleted its probe; uv confirmed the same existing
  path in `38.287 ms`; non-recursive cleanup and empty-parent cleanup passed. No build,
  install, demo, default-cache repair, product change, or push occurred. OS-level zero
  network attempts are not claimed. Result revision
  `94edd34cf05314d3515b27d3c33a8c6d91c2e8d1` passed all 471 local regression tests
  with 39 existing optional-dependency skips; the Git-mode public audit scanned 333
  files / 666 snapshots with zero violations.
- M44 froze its exact-revision explicit-cache packaging contract at
  `a4a98cd68900215c21f14aed1e38b0190eae9a5e`. Its sole attempt copied the frozen
  14,049-file / 627,590,722-byte cache subset with exact tree identity and started the
  offline build. A CP950 `UnicodeDecodeError` in subprocess output handling left
  `stderr` as `None`; runner receipt assembly then raised `AttributeError`. Read-only
  forensics found one 209,221-byte sdist with matching M40 presentation code, no unsafe
  or forbidden member, no wheel, and one inherited content failure: required `uv.lock`
  was absent. No fresh environment, install, installed demo, socket guard, retry, or
  push occurred. All disposable inputs and outputs were removed. Final regression and
  audit evidence is recorded in the M44 result after verification.
- GitHub Actions run [33486414127](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33486414127) passed every job except Python 3.11 because one test fixture used Python 3.12's generalized nested f-string grammar. The fixture now builds its TOML blocks before interpolation; local `ast` parsing under the 3.11 grammar and the focused tests passed. Follow-up run [33486674987](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33486674987) completed successfully across Python 3.11–3.14, the prerecorded-video contract, and the closed public demo.
- An isolated offline M9 package build produced the source archive and universal wheel and included the new Python adapter/evaluator modules without including VOST/VISOR data, model weights, local receipts, or evaluation configs/tools in the installed runtime data bundle.
- Source revision 2 is an 80-frame H.264/yuv420p generated replay with SHA-256 `b9cc79476d77f8d45acd1803c924de73914ffc4790f4da271f77cc8d4742eb43`. It was versioned because revision 1's encoding failed Chromium playback. Two consecutive local generations matched; browser QA then showed duration 8 seconds, ready state 4, scoped revision-2 content, and zero console errors.
- The source-revision-2 RGB smoke baseline measured AP50/key recall `1.0`, mAP50:95 about `0.7293`, zero false positives, zero clip-local ID switches/fragmentations, relation event F1 `1.0`, and the expected two-step answer. These are generated-artwork measurements only.
- `uv build` produced a source archive and universal wheel. A new Python 3.12 environment installed `whole_home_agent-0.1.0-py3-none-any.whl[demo]`, then ran `demo-recorded` outside the checkout with a `COMPLETE` receipt, two accepted estimated claims, and `FOUND sofa` under `source:b1-key-bag-sofa@2`.
- GitHub verified `kr-yep/whole-home-agent-public` as public with `main` as its default branch. Seed commit `21d057569073e7f2e16631780a8ab1150c2920f9` contains the clean release history.
- GitHub Actions run [32803963091](https://github.com/kr-yep/whole-home-agent-public/actions/runs/32803963091) completed successfully for the seed commit across configured Python 3.11–3.14 jobs.
- M2 through the first M6 push had green B0 jobs but failed the Linux video/demo jobs because Windows-generated annotation bytes used CRLF before Git normalized them to LF, invalidating the manifest hash in CI. Run [33447377312](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33447377312) exposed the same failure after the M6 push. The generator now emits canonical LF, and a regression test checks both generated text artifacts.
- GitHub Actions run [33447921546](https://github.com/kr-yep/whole-home-agent-public/actions/runs/33447921546) completed successfully for canonical-LF commit `4117e933b4b6076a5cda9130f61ea09ec998934b`: Python 3.11–3.14 B0 jobs, the prerecorded-video contract job, and the closed-demo job all passed.
- Archify v2.16.0 produced the proposed system and data-flow diagrams. Both passed 9/9 showcase checks with zero composition errors/warnings, desktop containment at 1440×900 through 2048×1320, and human inspection of light/dark captures. Exact source/artifact hashes and evidence limits are recorded in `docs/b0-b1-architecture-plan.md`.
- VISOR screen v1 froze `P01_03` (development, 61 frames), `P03_11` (validation, 29), and `P14_05` (test, 22) before results. Official Bristol archives and annotations passed full size/SHA-256 verification. The adapter preserves source frame indexes, maps only explicit COCO-compatible labels, and reports six boundary-clipped polygon points across five mapped objects under an 8-pixel tolerance without rewriting source data.
- On the RTX 4070 Laptop GPU, RetinaNet ResNet50 FPN v2 improved validation recall@0.5 from SSDLite320's `0.1429` to `0.2500`; validation 0.1–1% area recall moved from `0/3` to `1/3`. Detector p95 moved from about `47.4 ms` to `71.7 ms`, and peak VRAM from about `85.7 MiB` to `393.3 MiB`. The first and now locally locked test run improved overall recall from `0.1429` to `0.3929`, but both missed the test's only 0.1–1% target. VISOR active-object labels are not exhaustive scene labels, so generic-detector false positives and AP are not treated as exhaustive precision evidence.
- The validation-only four-tile SSDLite candidate reduced validation recall from `0.1429` to `0.0357`, left 0.1–1% target recall at `0/3`, and increased detector p95 from about `49.0 ms` to `243.5 ms`; peak VRAM stayed about `85.7 MiB`. It failed both the quality and `200 ms` cost gates and is rejected without touching the frozen test.
- VOST motion screen v1 pins `8253_open_bag` (development, 66 frames) and `5182_open_box` (validation, 136) from the official train/validation partitions. Its downloader verified the public S3 object version/ETag, a `21,042,989`-byte central directory with SHA-256 `7685955fd97adaade763a9a8f667ab8589b8fd63bac596eb207697ac68ed6d34`, and 404 selected JPEG/mask files totaling `52,172,072` bytes with canonical manifest SHA-256 `3a4b440fd2e4d5d45363a9daf2c986799ca14a9bee0578a29f910ae41ec7863f`. It also retains ignored upstream license, README, and split-list evidence; no third-party bytes enter Git.
- Development-only thresholds `0.005`, `0.01`, `0.02`, and `0.03` each selected `33/66` frames with 100% same-or-following mask-change coverage, so the deterministic tie rule froze `0.03`. On validation, scheduled RetinaNet-FPN reduced calls from `136` to `52` (`61.8%`), covered `40/41` mask-change events within one 5 fps frame (`97.6%`), measured `65.15 ms` detector p95, `0.58 ms` scheduler p95, about `352.2 MiB` peak VRAM, and pipeline real-time factor `0.16` versus `0.35` full frame. All declared bounded gates passed.
- The VOST result is scheduler-selection evidence only. Exact-frame validation coverage was `43.9%`; both sources are egocentric; the selected bag/box mask is not a COCO target-detection contract; and no object identity, containment, physical movement truth, fixed-camera transfer, live stream, or household claim is established.
- M10 froze 240 additional local-only VOST JPEG/mask files totaling `39,189,582` bytes with canonical hash `1e9f53eb1c0e302a0eea54cb5d9bff86d5029ee207b6bb7168c6260a199de060`. The official-train development source `3518_unscrew_bottle` contains 51 paired frames; the official-validation source `3510_unscrew_bottle` contains 69 and remained reserved. Fixed sampled offsets visually aligned target mask ID 1 with the visible bottle; this is an agent precheck, not source-authored COCO class truth.
- On clean code revision `581507229d43808fa6e4072b8b82ad98f8946268`, development full-frame recall@0.5 was `0.1961`, AP50 `0.1436`, and mAP50:95 `0.0808`; the tracker recorded five ID switches and four fragmentations. Scheduled inference covered `0.2051` of target mask-change events within one frame versus `0.2821` full-frame, retained `0.7273`, and avoided `0.4902` of calls. Detector p95 was `73.76 ms`, scheduler p95 `0.74 ms`, peak VRAM `412,383,744` bytes, and full/scheduled real-time factors were `0.416/0.246`. The predeclared development gate returned `REJECT_ON_DEVELOPMENT`; validation was not run and is not a zero score.
- On clean code revision `eb5d28a3bbd3bf552fb3e8d0bb00e8bc44c19e81`, the one-pass M11 diagnostic classified all 51 development frames: zero absent/void, zero without a bottle proposal at or above `0.05`, 30 with proposals only below `0.25`, 11 with product-threshold proposals but IoU below `0.50`, and 10 matches. Sixteen low-score frames were IoU-matchable, making the threshold-only matched-frame upper bound `26/51 = 0.5098`, below the unchanged `0.60` gate. The mask-box oracle tracker matched 51/51 observations for evaluation but produced 16 ID switches and zero fragmentations. Reserved validation was not loaded or run.
- On clean code revision `4aa0a2ad87dd100be5b38f0db5a01082ab023eb9`, the only M12 RF-DETR Small development attempt was valid. It matched `25/51 = 0.4902` target boxes versus the M10 full-frame comparator's `10/51 = 0.1961`, measured AP50 `0.2762`, mAP50:95 `0.1792`, `49.7820 ms` detector p95, and `149,987,840` bytes peak allocated VRAM. Recall failed the predeclared `0.60` gate while both strict cost gates passed, so the candidate is stopped. Paired descriptively, it matched 16/30 prior confidence-filtered frames, 0/11 prior localization-miss frames, and 9/10 prior matches. No second attempt, training, test source, reserved image/mask read, validation, movement candidate, claim commit, live/private/cloud/action connection, or `OPERATE` enablement occurred.
- On clean code revision `a01799b52ce4ffeeb239bbe6eb397afa2ff64bcf`, the only M13 real checkpoint-load attempt was valid. A community-converted D-FINE Small Safetensors artifact completed one excluded warm-up and 51 measured calls on one frozen in-memory generated RGB input. Canonical output was constant; p95 was `58.8077 ms`, maximum `62.4707 ms`, and peak allocated VRAM `104,530,432` bytes. Both strict cost gates passed with zero recorded socket attempts and zero public/private media bytes read. This proves only synthetic engineering compatibility: author-checkpoint parity, target-source accuracy, indoor transfer, stable runtime, tracking, movement, and product suitability remain unmeasured. The decision is `STOP_DFINE_SMALL_NO_DEVELOPMENT_SCREEN`; VOST/VISOR bytes remained unread.
- M14 used only official paper, repository, release-log, license, and artifact metadata. D-FINE Medium passed the narrow D-FINE-S same-table subgate (`+3.8` AP75, `+4.1` AP-small), while RT-DETRv2 Small failed it (`-0.5/+1.1`). Neither supplied a material same-protocol comparison against the target-screened RF-DETR Small path, and generic COCO proxies do not establish fixed-camera indoor transfer. The frozen TOML's omission of that Goal-level comparator was recorded as `FAIL_CLOSED_SCOPE_GAP`; no threshold was changed. The bounded result is `STOP_MODEL_SWAPPING`, with zero new model downloads/loads, media reads, screens, training runs, movement candidates, claim commits, or operational capability.
- Passing tests supports only the tested code, fixtures, interpreter, and environment. It does not support CV accuracy, household transfer, real-time performance, privacy compliance, production readiness, or physical truth.

## Recorded directions

| ID | Direction | Status |
|---|---|---|
| `PUB-DIR-001` | Create a separate clean public repository; do not expose or rewrite the private repository's history | Active |
| `PUB-DIR-002` | Keep `OPERATE` disabled until roles, consent, risk classification, safety boundaries, enforcement, and separate activation are complete | Active |
| `PUB-DIR-003` | Start from a minimal B0 baseline; separate data, control, action, authority, and physical outcome | Active |
| `PUB-DIR-004` | Use MIT for original public repository code/docs; review third-party artifacts separately | Active |
| `PUB-DIR-005` | Keep B0 as the only claim-commit/query core; make prerecorded perception a replaceable candidate-producing adapter | Implemented and locally verified on one project-generated synthetic replay; no real indoor transfer claim |
| `PUB-DIR-006` | Deliver a reproducible public demo, installable package, implemented-code diagrams, and release audit without widening sensing or action scope | Implemented, locally verified, and verified by public CI within the recorded envelope |
| `PUB-DIR-007` | Acquire a small lawful public indoor substrate, freeze source-video splits before results, compare paired real detectors, and keep all third-party bytes out of Git | Implemented and locally screened within the VISOR CC BY-NC non-commercial envelope; no product-transfer claim |
| `PUB-DIR-008` | Acquire a lawful consecutive public substrate without the full corpus, select scheduler settings on development only, and measure validation change coverage versus FPN cost | Implemented and locally screened within the VOST CC BY-NC-SA non-commercial envelope; no target-detection, fixed-camera, or product claim |
| `PUB-DIR-009` | Before creating movement candidates, freeze an explicit VOST bottle mask-to-label development/validation gate and reject the observation path if target detection or clip-local tracking is inadequate | Implemented; rejected on development without running validation. This stops only the frozen RetinaNet/IoUTracker/scheduler path on the declared source |
| `PUB-DIR-010` | Localize the rejected M10 development path with one non-product low-score diagnostic view and a separate mask-box tracker oracle before choosing a replacement | Implemented; confidence filtering was the largest detector symptom, threshold-only inclusion cannot meet the existing gate on retained outputs, and the oracle exposed an independent tracker blocker |
| `PUB-DIR-011` | Freeze exactly one off-the-shelf detector replacement, repair its real sparse-label/output contract, and permit one clean development screen only if artifact, offline, cost, and stopping rules are executable | Implemented; RF-DETR Small improved the finite-sequence match count and passed cost, but failed the unchanged recall gate. It is stopped without retry or reserved validation |
| `PUB-DIR-012` | Separate synthetic detector engineering compatibility from scientific priority before spending another target-source development read | Implemented for one D-FINE Small community conversion; engineering cost/determinism passed, but parity and stronger relevant prior did not. No VOST development screen is authorized |
| `PUB-DIR-013` | Stop the off-the-shelf detector tournament unless one candidate has materially stronger, same-protocol and target-relevant evidence than the failed path | Implemented as a no-media M14 decision; D-FINE Medium and RT-DETRv2 Small are not selected. The next work is a target-domain data/training reality gate, not another model preflight |
| `PUB-DIR-014` | Require every target substrate to pass the frozen official-source, localization, terms, acquisition, split, manifest, and bounded-cost gates before selection | Implemented as a no-media M15 decision; HOMAGE, CAD-120, and Watch-n-Patch are all selection-ineligible. The next work proves the target-label oracle and metric semantics without media before choosing a generation strategy |
| `PUB-DIR-015` | Put a complete D1 label-oracle and protected source-group validator before any target media generation or acquisition | Implemented with exact synthetic conformance cases; it validates measurement semantics only. The next work compares generation strategies without installing, downloading, rendering, or training |
| `PUB-DIR-016` | Select at most one D1 generation route only after exact oracle, provenance, split, delivery, storage, and dependency feasibility all pass | Implemented as a no-media M17 decision; the existing project-owned vector renderer is the sole bounded selection. The next work is exactly one tiny deterministic D1 substrate, not training |
| `PUB-DIR-017` | Prove one project-owned image/annotation generation seam before scaling synthetic data or training | Implemented as M18 within a 103,957-byte vector slice. It passes mechanics and reproducibility only; the next work must secure a real transfer oracle before any model experiment |
| `PUB-DIR-018` | Select at most one lawful compact real transfer oracle through current official rights, acquisition, complete-scoring, protected-group, small-target, D1, and cost evidence | Implemented as M19; BOP YCB-V BOP'19 is the sole bounded acquisition choice after the frozen final cost tie-break. Selection is not an accuracy or movement claim |
| `PUB-DIR-019` | Materialize the selected source only after immutable byte identity, full ZIP header safety, synthetic translator conformance, and unchanged D1 rules pass | Implemented as M20; byte identity and synthetic conformance passed, but the frozen single-root archive rule failed before extraction. One per-archive root-mapping repair is proposed without changing scientific gates |
| `PUB-DIR-020` | Retry YCB-V materialization exactly once with per-archive source/destination roots and unchanged scientific predicates | Implemented as M21; the infrastructure repair passed, but the combined real annotation predicate returned no match. The next gate may only localize the failed conjunct, not retry or loosen it |
| `PUB-DIR-021` | Diagnose the M21 predicate terms independently on the exact same annotations without media, materialization, or model work | Implemented as M22; both terms are present but disjoint by scene. A separate validity decision is required before any cross-scene slice |
| `PUB-DIR-022` | Permit cross-scene pairing only when authoritative detection aggregation, complete-negative semantics, split protection, paired comparability, and small-slice uncertainty all pass | Implemented as M23; all five gates pass for one test-only diagnostic oracle capped at 18 frames. This is not training, transfer gain, movement, relation, or whole-home evidence |
| `PUB-DIR-023` | Materialize only the smallest source-ordered cross-scene pair and preserve each scene as a separate source sequence | Implemented as M24 with deterministic ignored output; the newly observed visible-pixel versus bbox-area mismatch blocks a detector experiment until a separate dual-area diagnostic passes |
| `PUB-DIR-024` | Require the same reference annotation to pass both visible-pixel and M16 visible-bbox small-area predicates before materializing a detector oracle | Implemented as M25; two eligible frames exist and the exact source-ordered object-4 pair is selected for one replacement materialization only |
| `PUB-DIR-025` | Replace the metric-misaligned M24 fixture only with the exact M25 pair and require one actual M16 small-bbox target | Implemented as M26 with deterministic ignored output; this passes fixture mechanics but does not authorize model work or a gain claim |
| `PUB-DIR-026` | Select the primary judge demo through clarity, reproducibility, end-to-end value, trace, dependency, claim, and safety gates while separating product demo, CV smoke, and future science | Implemented as M27; existing synthetic E2E is primary, M26 is optional smoke, and model gain is deferred to a separate protected-group contract |
| `PUB-DIR-027` | Accept the primary demo only through two offline interfaces, exact source/answer/trace checks, and non-leading synthetic metrics; preserve frozen mismatches rather than rewriting them | Implemented as M28 normal stop. The interface outputs agree and two presentation gaps are hardened; M29 may correct only event-label/evidence-window meaning and retry once |
| `PUB-DIR-028` | Correct the event-label/evidence-window meaning before exactly one committed retry, and prohibit post-attempt expected-value changes or a second retry | Implemented as M29 normal stop. Corrected timing checks pass; only the absent top-level answer `subject_id` remains for a separate no-media schema decision |
| `PUB-DIR-029` | Choose the answer-subject boundary through canonical identity, authoritative source, B0/B1 parity, non-FOUND completeness, no-drift, compatibility, clarity, and bounded-cost gates | Implemented as M30; canonical `AnswerTrace.subject_id` is the sole selection for one separately frozen M31 implementation |
| `PUB-DIR-030` | Implement query-subject identity once at canonical `AnswerTrace` and project it through both serializers without changing session semantics | Implemented mechanically in M31; all semantic gates pass, but M31 normally stops on a contradictory full-suite/no-media verification boundary |
| `PUB-DIR-032` | Give static contracts, complete regression, and ad-hoc acceptance/experiment distinct fixture authority | Implemented in M32 for the exact committed D0 synthetic demo; it changes no M31 history or operational authority |
| `PUB-DIR-033` | Before claiming teammate usability, require one disposable public clone, locked install, offline exact-answer/trace drill, bounded receipt, and cleanup | Implemented as an M33 normal stop before install; exact clone and cleanup passed, but clean-install usability remains unverified |
| `PUB-DIR-034` | Treat M33's full-string uv version comparison as infrastructure only; permit at most one separately frozen semantic-version parser retry with all product gates unchanged | Implemented in M34; parser/install/demo mechanics passed, text-lock raw-hash portability stopped the contract, and no retry remains |
| `PUB-DIR-035` | Do not use raw checkout bytes as cross-platform identity for versioned text until a repository-only portability decision selects a stable meaning | Implemented in M35; Git blob identity is selected for one separately frozen checker-only M36 hardening |
| `PUB-DIR-036` | Harden the teammate checker to use committed Git-blob identity while preserving exact revision, clean worktree, and non-Git failure | Implemented in M36 with focused temporary-repository tests and full regression; M34 remains STOP |
| `PUB-DIR-037` | Give a real teammate exact cross-platform commands, receipt semantics, troubleshooting, guarded cleanup, presentation fallback, and a sanitized report without manufacturing external evidence | Implemented as the M37 document gate; no real teammate receipt exists |
| `PUB-DIR-038` | Consolidate representative whole-home products and research into a smallest useful demo, implement one independently verifiable task, and prepare the next without widening runtime authority | Implemented as M38: market adopt/defer/reject decisions, ADR 0020, and a local minimized text-context preview. M39 is prepared as a separate provider/egress decision; no provider code or egress was added |
| `PUB-DIR-039` | Decide the smallest replaceable language-presentation boundary without treating a key as authority or adding provider code | Implemented as M39: local default, exact field classification, blocked cloud preconditions, deterministic fallback, and honest pure-local/hybrid labels |
| `PUB-DIR-040` | Implement the M39 local default through one narrow port without widening runtime or authority | Implemented as M40: exact admission/output validation, deterministic relation-only prose, sanitized fallback, and unchanged structured truth path |
| `PUB-DIR-041` | Verify the exact M40 package and installed demo once, offline and outside the checkout, before any publication decision | Implemented as an M41 normal pre-artifact stop. The single build attempt hit an unresolved default-cache initialization error; no retry, alternate cache, package claim, public-CI claim, or push is authorized |
| `PUB-DIR-042` | Before any new packaging gate, test one exact ignored repository-local uv cache path without building or repairing the default cache | Implemented as an M42 normal pre-probe stop. uv accepted and reported the explicit path but did not create it; caller-created cache semantics require a separate decision |
| `PUB-DIR-043` | Prove caller-created, confined, writable, empty, uv-selected, and non-recursively cleanable cache semantics before packaging | Implemented as an M43 bounded no-build pass. Only a separately frozen M44 explicit-cache packaging gate may follow |
| `PUB-DIR-044` | Attempt the exact M40 package once with the M43 explicit-cache semantics, and fail closed before install or publication on any runner/artifact mismatch | Implemented as an M44 normal stop. Preserve the decode failure, missing-wheel state, and missing `uv.lock`; no retry or push follows from this gate |
| `PUB-DIR-031` | Every milestone push must visibly state what is usable now, what is still missing, and what changed in that push | Active; README handoff block and `docs/current-capability-status.md` are the public handoff surface |

## Open gates and blockers

- Named project, policy, engineering, and data roles remain unassigned.
- The full B0 conformance, recovery, runtime-path, performance, and independent maintainer exercise gates are incomplete.
- The B1 candidate-source seam, generated video/decoder/scheduler slice, conservative binding/relation/query rules, and closed CLI/Streamlit presentation remain unchanged. VISOR and VOST are separate evaluation adapters, not claim-producing sources. VISOR is sparse egocentric kitchen data with active-object rather than exhaustive-scene labels; VOST is consecutive egocentric transformation data whose selected mask is not a COCO class contract. Neither supplies the key→bag→sofa relation, fixed-camera transfer, live-source evidence, or operational capability. The RF-DETR Small development result is rejected; the D-FINE Small result is synthetic engineering evidence only; M14 stops D-FINE Medium and RT-DETRv2 Small from extending the model tournament. M15 selects none of HOMAGE, CAD-120, or Watch-n-Patch. M16 adds only an evaluation oracle. M17 selects only a vector generation strategy, M18 proves its bounded substrate mechanics, M19 selects only one real transfer-oracle acquisition route, M23 permits only a tiny cross-scene test oracle, M24 materializes a metric-misaligned pair, M25 selects an exact metric-aligned replacement, M26 materializes it as one M16 small-bbox smoke target, M27 keeps that smoke separate from the selected synthetic product demo and deferred scientific lane, M28 hardens presentation while preserving its trace mismatch, M29 exposes an answer-subject gap, M30 selects its canonical boundary, and M31 implements it while preserving a verification-boundary stop. No detector or dataset adapter is promoted into the semantic path.
- No household data class, person, room, camera, credential, endpoint, device, or capability is enrolled.
- Consent, retention, deletion, access, incident, kill-switch, and independent enforcement mechanisms do not exist.
- The M38 text packet is verified only on the fixed synthetic replay. M39 classifies its
  fields and proposes retention, credential, network, and failure constraints, but no
  policy authority or credential owner is assigned and no provider is implemented.
- M40 proves only deterministic local prose and failure fallback in the closed synthetic
  demo. M41 did not reach artifact creation, so it does not establish installed-wheel
  packaging of the new module, clean-install behavior, model quality, or provider
  compatibility.
- M42 establishes path-option acceptance only. It does not establish cache creation,
  writability, packaging recovery, or M41's default-cache root cause.
- M43 establishes caller-created cache mechanics only. It does not establish dependency
  availability, artifact contents, clean install, demo behavior, teammate success, or
  public CI.
- M44 does not establish packaging recovery. Its runner retained no build return code,
  the only sdist fails the inherited `uv.lock` content requirement, no wheel exists, and
  installed M40 presentation/demo behavior remains unverified.

## Next safe action

Stop after M44. If work resumes, prepare a repository-only M45 decision that preserves
the failed attempt while hardening subprocess output capture against non-CP950 bytes and
deciding how the required `uv.lock` enters an sdist. Add a non-ASCII output regression
without rebuilding. Any new package execution must be separately frozen; do not infer a
retry or push from M44, use a provider/private data, or change runtime authority.
